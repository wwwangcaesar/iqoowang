# B｜分时图经验规则·步骤5+6 实现记录

**记录人**：B（本文档及对应代码改动以"B"署名）
**认领任务**：`D-分时图经验规则实现方案.md` 中明确"留给B"的步骤5（AIContextBuilder+LocalAIAgent Context Block）、步骤6（涨停巨量新检测点）——用户直接指定，非B自行扩大认领范围
**状态**：已实现（源码改动完成，未做Gradle编译验证）
**依据**：`D-分时图经验规则实现方案.md` 第4节（通道B设计）、第2.2节（涨停巨量检测点）

---

## 1. 现状确认（动手前重读的文件）

D的实现进度比文档表面状态更新：认领表写的步骤4"进行中"，但B实际读代码时发现`checkUnderwaterReversal()`/`RuleResult.underwaterReversal`已经完整实现，`IntradayPatternAnalyzer.java`三个方法（`detectVReversal`/`checkBreakoutVolume`/`checkVolumeSurgeAtLimit`）也已就位，字段签名与D文档描述一致（`checkVolumeSurgeAtLimit`签名已经是简化版，不接收流通股本参数——B之前建议的"从stock_list_cache推算floatShares"方案，D核实`MarketDataManager.getStockCap`是private后未采用，直接降级为只算成交量倍数，B步骤6沿用这个已定的降级方案，不重新折腾）。

另外确认了一件不在B/D原计划内、但已经生效的背景变更：`evaluateStarterGapDown`/`evaluateStarterGapUpOrFlat`/`evaluateAddHalf`/`evaluateFullPosition`四处都有"【2026-09-13改造，用户已明确要求】取消放量确认这道门槛"的标注——这是用户在其他会话里对买入逻辑做的独立调整，跟本次分时图经验规则功能无关，B未触碰这部分，只是如实记录看到的现状。

## 2. 步骤5：Context Block（通道B）

### 实现方式

`RealtimeQuoteManager`已有`getCachedMinuteLine(code)`——D此前为分时图前端功能加的缓存读取方法（常规监控tick已经拉过分时数据，这里只读缓存，不额外发网络请求）。`AIContextBuilder`新增`buildIntradayPatternContext(code)`：读这份缓存，调`IntradayPatternAnalyzer.detectVReversal()`，把检测到的尖角反转（方向/量能比/维持分钟数/是否强确认）格式化成一行文本，没检测到就返回空字符串（不占位）。

`LocalAIAgent.buildVerifyPrompt()`里"量化指标："那一行之后接一行"分时形态："，取值来自上面这个方法，取失败（try/catch）不影响整条prompt组装，跟其他既有的安全降级写法（`safeWisdomBlock`/`safeTradeLessonBlock`）一致。

### 与D步骤4（水下反转通知）的关系

两处都会调`detectVReversal()`，但用途不同：D的`checkUnderwaterReversal`是"检测到就触发一次性通知+AI完整分析"，本步骤是"每次verifySignal调用时都带上当前状态供AI参考"——互不干扰，各自独立调用，没有共享状态或互相依赖。

## 3. 步骤6：涨停放巨量出货检测

### 与D文档建议的一点差异，说明原因

D文档建议把这个检测点放在`RealtimeMonitorService`的tick循环里。B改为放进了`TradingRuleEngine.evaluate()`的"持仓：止损优先"区块内，止损未触发时追加检查——原因：判断"今日是否涨停"需要`LimitInfo.atUp`，这个字段是`evaluate()`内部`computeLimitInfo()`算出来的，`RealtimeMonitorService`并不直接持有这份数据，放进`evaluate()`里可以直接复用已经算好的`limitInfo`，不需要为了这一个检测点在两个类之间重新传递或重算涨停状态。功能上完全等价于D的设想（对持仓中的涨停股检查放量倍数），只是host的文件不同，写进这里作为过程记录，供后续有疑问时对照。

### 实现方式

`TradingRuleEngine`新增`checkLimitUpVolumeSurge(code, quote, limitInfo)`：`limitInfo.atUp`为false直接跳过；为true则取近10日K线（排除"今天"这一条，防止盘后已下载过一次的边界情况），调`IntradayPatternAnalyzer.checkVolumeSurgeAtLimit(quote.volume, recentVols)`，倍数落在`DISTRIBUTION`区间（≥3倍，B研究1.3节的出货阈值）时返回`WARN_PRESSURE`（用户已确认级别，不是`STOP_LOSS`）。`evaluate()`里在`evaluateStopLoss()`返回`NONE`之后调用这个新方法，两者不会同时触发同一支股票（先到先得，止损优先级更高）。

## 4. 未做的事

- 换手率计算：数据源不可用（`MarketDataManager.getStockCap`是private，D已核实），只用成交量倍数判断，note里明确写了"换手率数据源暂不可用"，不假装算过。
- 未改动D步骤3/4的任何代码，未改动`evaluateStarter*`/`evaluateAddHalf`/`evaluateFullPosition`本身的判断逻辑。
- 未做Gradle编译验证——B没有编译/设备访问能力，建议下次构建时留意。

## 5. 变更记录

- **2026-09-15　B**：认领`D-分时图经验规则实现方案.md`留给B的步骤5、步骤6（用户直接指定范围，不含步骤4）。重读`IntradayPatternAnalyzer.java`/`RealtimeQuoteManager.java`/`LocalAIAgent.java`/`AIContextBuilder.java`/`TradingRuleEngine.java`全文确认D步骤1-4的实际完成度（比文档认领表状态更新，步骤4已完整实现非仅"进行中"）。实现步骤5：`AIContextBuilder.buildIntradayPatternContext()`+`LocalAIAgent.buildVerifyPrompt()`接入"分时形态："一行。实现步骤6：`TradingRuleEngine.checkLimitUpVolumeSurge()`，放在`evaluate()`持仓止损区块而非`RealtimeMonitorService`（原因见第3节），换手率降级为只用成交量倍数（沿用D已核实的降级方案）。未做Gradle编译验证。
