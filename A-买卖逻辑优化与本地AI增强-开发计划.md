# A｜操盘手买卖逻辑优化与本地AI增强 —— 开发计划

**记录人**：A（本项目由多个AI协同开发/审查/规划，本文档及后续对应的修改记录均以"A"署名）
**状态**：用户已对第4节全部问题拍板确认（R2/R3/R4/R6/R7均已确认方案，详见各节及第4节标注）。当前进度：R12已由B实现，R8已由B实现（见变更记录），R11由C接手实施中，R4已由A实现（2026-09-08），R2/R3已全部实现（用户已对R3语义拍板），R6/R7已由A实现，R10仍待实现
**关联文档**：`操盘手经验终版.md`、`第二轮修改方案思路需具体审核.md`、`修改方案以及进度计划.md`
**依据**：用户于本次会话贴出的需求原文（完整保留于文末附录，供追溯）

---

## 0. 协作说明

1. 本文档只做规划，不包含任何代码改动。按项目约定的流程：先计划 → 审查AI复核 → 用户/审查AI确认第4节的开放问题 → A再动手改代码。
2. 日志系统重构（每日文件夹＋每股票独立日志文件＋7天保留）由另一位协作AI负责实现，本文档第2节的 R11 只记录目标规格用于跨AI协调，A不会改动 `DecisionLogger` 现有结构，涉及日志调用的地方一律沿用现有接口签名。
3. 下文所有代码引用均基于A本次会话中通过 Filesystem 连接器直接读取的当前源码（`TradingRuleEngine.java`／`TradingRuleConfig.java`／`RealtimeQuoteManager.java`／`WatchlistManager.java`／`RealtimeMonitorService.java`／`DatabaseManager.java`／`LocalAIAgent.java`／`AIContextBuilder.java`／`StockBridge.java`），并与 `操盘手经验终版.md` 全文做过交叉核对。

---

## 1. 现状确认：直接回答用户提出的5个问题

**Q1. 当前"分时均价(VWAP)"怎么算的？是不是盘中实时？**
是真实盘中数据，不是估算。`TradingRuleEngine.computeVwap()` 优先取 `RealtimeQuoteManager.fetchMinuteLine()` 返回的分时数据里最后一个分时点的 `avgPrice` 字段——这是腾讯分时接口服务端算好的"累计成交额/累计成交量"分时均价，与正规炒股软件里看到的分时均价线（黄线）是同一个东西，逐分钟点位可用于"最近N分钟有没有跌破"这类判断。只有这次没抓到分时数据时，才退化成用快照的 `成交额/成交量` 算一个等价的近似值（数学上就是当前时刻的累计VWAP，只是没有逐分钟明细，无法做"最近N分钟"类判断）。

**Q2. "27号VWAP"和"28号VWAP"会不会互斥导致买不进去？**
现在的代码里，低开路径（今开<昨收）只参照**昨日全天真实VWAP**一个基准，完全不涉及当日VWAP，所以低开路径本身不存在"两个VWAP打架"的问题。但高开/平开路径（今开≥昨收）现在完全只看**当日VWAP**，压根不参照昨日VWAP。这两条路径是分开评估、互不知道对方存在的——所以现状不是"卡死买不进"，而是"两个场景各自为战，没有一套随开盘时间推移动态切换权重的统一逻辑"。这正是本次需求要解决的问题，方案见下文 R2。

**Q3. 独立破位止损，参照的是"昨天最低价"吗？**
不完全是。现在优先用的是**形态日（仙人指路当天/选股当天）固定最低价**——这个值在股票进候选池那一刻就写死存起来，之后无论观察多少天都不会变。只有当股票是手动同步进持仓、没有形态日数据时，才退化成动态跟踪的"前一根阳线最低价"（会随新出现的更高阳线不断上移）。这两个都不是严格按自然日滚动的"昨天最低价"。这是本次需求里**最需要拍板**的一个分歧点，见 R4。

**Q4. 一级预警（涨幅回撤到峰值一半）算的是相对昨收，还是相对我的实际买入成本？**
是相对昨收（`prevClose`）算的，跟实际买入价完全无关——这也是"买入即触发减仓提醒"这种困惑感的根源之一，见 R6。

**Q5. 当天买入、T+1不可卖的仓位，止损/预警提示现在还会正常推送吗？**
会。现在只是在通知文案里加一句"T+1前不可卖"的提示，推送本身（震动、通知栏）不会因为100%锁仓而降级，见 R7。

---

## 2. 需求逐项梳理与技术方案

### R1．日期口径：交易日必须剔除法定节假日

**现状**：`RealtimeMonitorService` 判断候选自动移出天数、`MarketDataManager.computeExpectedTradeDate()` 等处目前只排除周六周日，不接入法定节假日日历（代码注释里承认了这个简化，理由是"节假日本来也没有行情变化，顶多让移出判断稍微推迟，不会误报"）。

**方案**：新增一份A股法定节假日静态表（JSON放 `assets`，每年更新一次即可，不需要联网抓取），替换所有仅按"周几"判断的地方。影响面：候选自动移出天数计算、"上一交易日"判断、以及若 R4 选择滚动昨日口径则独立破位止损也要用这份日历。

**优先级**：中——现状不会主动出错，R4已确认采用 `prevDay.prevLow`（逐日滚动，见R4），不依赖节假日日历本身；但候选自动移出天数计算等处仍建议尽快接入，避免节假日期间误判观察天数。

**用户补充需求（已确认）**：每年年底，App启动时增加一次性提醒弹窗，提示"该更换明年的法定节假日静态表了"（一年提醒一次即可，不需要每次启动都弹）。

**状态**：✅代码里已经实现（A于2026-09-10核对`TradingCalendar.java`/`StockMasterApp.java`/`MainActivity.java`/`RealtimeMonitorService.java`/`MarketDataManager.java`时发现）：新建的 `TradingCalendar` 单例类、`assets/trading_calendar.json`（已包含2026年数据）、三处集成点（`RealtimeMonitorService.isWithinTradingHours()`／`computeObserveDays()`、`MarketDataManager.computeExpectedTradeDate()`，均带明确的"【R1修复】"注释）、`StockMasterApp.onCreate()`里的`TradingCalendar.init()`（正确排在`MarketDataManager.init()`之前）、以及`MainActivity.checkTradingCalendarYearEnd()`实现的年底提醒弹窗，全部逐一读代码确认存在且逻辑正确，不是推测。同样没有找到对应的认领/变更记录——如果是你或B/C/D中的某位实现的，麻烦补一条记录。

---

### R2．底仓/加仓的VWAP参照统一设计（对应 Q2）

**用户方案复述**：刚开盘时当日VWAP尚不具统计意义，只有昨日VWAP是可靠参照；底仓门槛＝价格站上"当前可用的那个VWAP"，加仓门槛＝价格同时站上昨日VWAP与当日VWAP两者。

**需要拍板的三点**：

- **(a) 替换还是叠加？** 建议**替换**现有的"低开/高开分两条路径"设计，改成统一模型——8月20日那次重写本身就是很新的结构调整，如果再叠加一套"按时间切权重"的逻辑，会形成两套并存的判断口径，不利于后续维护。
- **(b) "当日VWAP还没有统计意义"的分界怎么量化？** 提案：新增独立配置项 `vwapDualRefSwitchMinutes`（默认30，与放量门槛用的 `earlyWindowMinutes=60` 是两回事、不复用），开盘30分钟内底仓只看昨日VWAP；30分钟后底仓判断改为"价格 ≥ min(昨日VWAP, 当日VWAP)"（两者取较松的一个）；加仓统一要求"价格 ≥ max(昨日VWAP, 当日VWAP)"（两者都要站上）。这一映射需要用户确认是否符合预期。
- **(c) 高开/平开时"昨日VWAP"这个门槛可能形同虚设**（高开通常本来就高于昨日VWAP），真正卡住底仓的核心其实是 R3 的"回踩当日VWAP+计时确认"。理解为：R2 主要解决**低开场景**下该看哪个VWAP的问题，高开/平开场景的核心改动是 R3，两者合起来才是完整方案——需要用户确认这个分工理解是否正确。

**状态**：✅已确认并完成（D，2026-09-10）——`evaluateStarterGapDown`改为开盘`vwapDualRefSwitchMinutes`（默认30）分钟内只看昨日VWAP、30分钟后取min(昨日,当日)；`evaluateAddHalf`加仓参照价统一改为max(昨日VWAP,当日VWAP)。详见 `D-VWAP双参照统一与重点监听确认-开发计划.md`。未做Gradle编译验证。

---

### R3．高开/平开路径重构：重点监听 + 计时确认

**需求复述**：高开/平开回踩到当日VWAP → 推送"进入重点监听"提示，该股票轮询频率减半并开始计时；**实际交易时间**满1小时（午休11:30-13:00不计入）仍未跌破 → 触发底仓；到收盘前15分钟仍未跌破 → 触发加仓。

**现状**：`evaluateStarterGapUpOrFlat` 是瞬时判断（只看最近10个分时点的最低价有没有跌破VWAP×0.999），命中即直接给出底仓信号，没有"进入监听状态""计时""轮询频率"这些概念。

**需要新增**：
1. 新状态字段（建议加进 `WatchlistManager` 候选记录持久化结构）：`focusWatchStartTime`（首次回踩确认时间戳）、`focusWatchStatus`（NONE/WATCHING/CONFIRMED）。
2. "实际交易时间满1小时"的计算：不能用挂钟时间差（会跨午休），需要新写一个"两时间戳之间累计交易分钟数，扣除11:30-13:00"的工具函数（当前代码库没有这个能力）。
3. **"轮询频率减半"建议不按字面实现**：现在 `RealtimeMonitorService` 是全部候选股共用一个全局tick（2分钟一次批量拉行情），这个设计本身是为了控制接口调用量、避免触发腾讯接口熔断。若为单只股票单独开快轮询，会破坏批量效率，且候选池里同时有多只股票进入重点监听时接口压力会叠加。建议：**计时用时间戳判断，不依赖tick频率**（现有2分钟节奏对"是否满1小时"这个量级的判断精度完全够用，误差最多一个tick周期）；如果目的是希望回踩确认本身更灵敏（而不只是计时准），可以做一个轻量补充——只对当前处于"重点监听"状态的少数股票，额外叠加一个更短周期（如90秒）的小批量拉取，不改动全局节奏。需要用户明确"轮询频率减半"具体想解决的是计时精度还是行情灵敏度。
4. 加仓触发时点"收盘前15分钟"建议新增独立配置项 `gapUpAddConfirmMinutesBeforeClose`（默认15），不与止损用的 `patternLowStopNotifyMinutes` 共用，避免以后调整其中一个连带影响另一个。

**状态**：✅已全部确认并实现。用户已直接拍板选定**A提的备选读法**（先底仓后加仓两次先后动作，非 D 原方案“同一信号两次确认机会”）。经 A 核实，`TradingRuleEngine.java` 现代码已正确实现此读法：`evaluateStarterGapUpOrFlat` 只保留“满1小时确认底仓”一个触发点（设`focusWatchStatus=CONFIRMED`）；`evaluateAddHalf` 新增独立的 `focusWatchGraduation` 分支（检查`focusWatchStatus==CONFIRMED`且同日且已到收盘前`gapUpAddConfirmMinutesBeforeClose`窗口且价格仍≥VWAP）才触发加仓，与原有`breakWater`/`pullbackHold`两条路径是或关系、不冲突。未做Gradle编译验证。

---

### R4．独立破位止损参照价口径（对应 Q3，本轮最需慎重的一项）

**现状**：8月20日重写把止损参照价从"动态前一根阳线最低价"改成"形态日固定最低价"，目的是避免长期观察/持仓导致止损线不断漂移。

**本次用户描述**：反复使用"昨天（滚动、不分阴阳线）最低价"。两种口径在"选股后当天/次日即买入"的场景下结果通常一致，分歧只在"观察多日后才买入"或"持仓多日"的场景下显现——而这正是8月20日那次改动想解决的场景。

**三个选项，需用户明确选择**：
- **选项A（维持现状）**：继续用形态日固定最低价，仅优化通知文案（把"形态日"换成具体日期，如"2026-08-27最低价¥X.XX"），减少理解歧义，底层逻辑不变。
- **选项B（改为滚动昨日）**：完全按本次描述，参照价改为动态更新的前一交易日最低价（不分阴阳线）。代价：重新引入"止损线随大盘整体回撤不断下移"的风险，即8月20日那次修复要解决的问题会复现。
- **选项C（折中，A倾向）**：止损参照价＝`max(形态日最低价, 前一交易日最低价)`，取两者中较高（更严格）的一个——既保留形态日"锚点不漂移"的优点，又能响应"跌破昨日最低价就该有反应"的诉求。

**状态**：✅已确认选项C，且已由A实现（2026-09-08，见变更记录）。`evaluateStopLoss` 的独立破位参照价已改为 `usePatternLow && usePrevLow ? Math.max(pattern.low, prevDay.prevLow) : (usePatternLow ? pattern.low : prevDay.prevLow)`，`state.prevYangLow` 字段未受影响，继续供候选股清理逻辑使用。尚未做Gradle编译验证。

---

### R5．T+1新买入次日反弹止损（对应用户自行推导的结论）

用户在需求文本中自行把这个场景归结为"就是（R4）规则本身：次日收盘仍收不回参照最低价 → 无条件离场"，A认同这个结论，**不需要单独新写机制**。只需确保：
- T+1锁仓期间止损信号本身该产生仍要正常产生（只是文案标注"暂不可卖，等解锁/等明天开盘执行"），不能因为"今天卖不掉"就连信号都不生成，否则解锁当天反而没有历史信号可参考；
- 通知文案把"要不要止损"和"现在能不能实际卖出"两件事分开表述，避免误解成两套逻辑。

**状态**：无需单独实现，随 R4 选定结果自动生效。

---

### R6．一级预警（WARN_PRESSURE）重新校准（对应 Q4）

**现状**：`peakGainPct`／`currentGain` 均相对 `prevDay.prevClose` 计算，与 `Position.avgCost`（实际持仓成本）无关。

**两个方案**：
- **方案A（纯展示增强，风险最低）**：触发条件完全不变（继续用操盘手经验原文3.3节"相对昨收峰值回撤50%"的定义），但推送文案与决策日志里同时打印"当前实际持仓浮盈浮亏"（用 `Position.avgCost` 现算），让用户能一眼分辨这次预警是"股票本身走弱"还是"买入价偏高叠加的错觉"，不改变任何触发逻辑。
- **方案B（改动触发逻辑）**：仅当"相对昨收峰值回撤"与"相对实际成本浮盈同时转弱"两者都满足才推送，否则静默记录。好处是减少噪音感，但偏离了操盘手经验原文的定义，与用户本次强调的"严格按原文实现、不要自行加限制条件"这条原则存在张力。

**A建议**：先上方案A观察效果，如果仍觉得频繁，再讨论方案B，不代表已拍板。

**A认领并给出具体实现位置（2026-09-10）**：不需要改 `TradingRuleEngine.evaluateStopLoss` 的方法签名——`RealtimeMonitorService.evaluateAndAct()` 里已经在算 `pos`／`holding`／`holdCost` 这几个值（就在 `applyT1Note(result, pos, holding)` 调用的同一个位置），新增一个 `applyCostBasisNote(result, pos, holding, holdCost, quote)` 方法，紧跟在 `applyT1Note` 后面调用，只对 `result.action == WARN_PRESSURE` 生效，往 `result.note` 追加一句"你的实际持仓：成本¥X.XX，现价¥Y.YY，浮盈/浮亏Z.ZZ%"。只改 `RealtimeMonitorService.java` 这一个文件，不碰 `TradingRuleEngine.java`，改动范围很小。

**状态**：✅已确认方案A，已由A实现（2026-09-10）。`RealtimeMonitorService.java` 新增 `applyCostBasisNote`，在 `applyT1Note` 之后调用，仅对 `WARN_PRESSURE` 生效，不碰 `TradingRuleEngine.java`。尚未做Gradle编译验证。

---

### R7．T+1锁定仓位的卖出提示降噪（对应 Q5）

**现状**：无论T+1锁仓比例多少，止损/预警命中都走完整推送流程（震动+通知栏），只在文案里附带提示。

**方案**：当 `DatabaseManager.getSellableQuantity(code)==0`（持仓100%为当日买入、一股都不能卖）时，信号推送降级——正常写决策日志、正常更新候选池 `PENDING` 状态（方便事后在App查看），但不发高优先级通知（不震动/不弹顶部通知，改为低优先级静默通知或列表角标）；待次日该部分份额解锁后的下一个常规tick，若止损条件仍成立，按正常优先级重新推送（复用现有tick循环即可，不需要为"解锁瞬间"专门写触发器）。

**A认领并给出具体实现位置（2026-09-10）**：Android 8+ 的通知优先级主要由Channel的importance决定，仅在 `NotificationCompat.Builder` 上设 `setPriority()` 不会真正降噪，所以需要新增一个独立的低优先级通知渠道 `CHANNEL_ID_QUIET`（`IMPORTANCE_LOW`，关闭震动），在 `createChannel()` 里一并创建。`evaluateAndAct()` 里 `fireAlert(...)` 调用前新增判断：`action` 是 `STOP_LOSS`/`WARN_PRESSURE` 且 `DatabaseManager.get().getSellableQuantity(item.code) <= 0`（持仓100%今日买入）时，走新方法 `fireQuietAlert(...)`（发到 `CHANNEL_ID_QUIET`、不震动）而不是原来的 `fireAlert`；其余分支不变。角标部分依赖前端读取候选池PENDING类状态展示，后端数据本来就已经暴露（`toJson()`），本次不改前端，做法上跟R11先做后端、前端另行确认保持一致。A已确认，即将实现。

**状态**：✅已确认低优先级静默通知+角标方案，已由A实现（2026-09-10）。`RealtimeMonitorService.java` 新增 `CHANNEL_ID_QUIET`（`IMPORTANCE_LOW`）和 `fireQuietAlert`，`evaluateAndAct()` 中根据 `getSellableQuantity<=0` 分流到 `fireQuietAlert`/`fireAlert`。角标部分依赖后续前端工作。尚未做Gradle编译验证。

---

### R8．日志可观测性改进（可直接实现，无需等待确认）

用户举的"五洲交通 现价¥4.38＝VWAP¥4.38 却提示未回踩确认"这个例子，A的判断（基于代码逻辑推断，未拿到当日实际分时数据核实）：大概率是最近10个分时点里的**最低价**（`minRecent`）曾跌破 `VWAP×0.999`，即便当前瞬时价格已回到VWAP，"最近10分钟的低点是否守住"这条件仍未满足，所以判定"未确认"——这是设计意图（区分"当前刚好在VWAP"与"最近确实守住没破位"），不一定是bug，但现有文案没有把 `minRecent` 这类中间值打出来，导致无法直接验证。

**方案**：`detectDivergenceKline`、`evaluateStarterGapUpOrFlat` 等方法生成note文案时，把用于判断的中间值（`minRecent`、阈值、当前 `quote.price`）一并打印，不再只给结论句。纯粹增加日志明细，不改变任何判断逻辑。

**状态**：✅已由B实现（2026-09-03，见变更记录），范围比本节原方案更完整——`evaluateStarterGapUpOrFlat`／`evaluateAddHalf`／`evaluateFullPosition`／`detectDivergenceKline` 都已带上具体中间值和分级说明，`evaluate()` 里STARTER/ADDED状态的提示也会把满仓/加仓两条路径各自差多少拼出来。A于2026-09-08核实`TradingRuleEngine.java`时确认了这个实现已存在，未重复劳动。

---

### R9．阈值基调：严格按原文实现，不叠加额外保守条件（原则性条款）

本轮所有改动只针对 `操盘手经验终版.md` 中已写明的规则与本次补充需求，不允许因为"这样更稳妥"而顺手加码额外止损位、额外确认时长或更高的放量倍数。若实测发现按原文实现确实偏激进，由用户决定是否收紧，不能由A单方面先斩后奏。适用于以上所有改动，不单独排期。

---

### R10．收盘前20分钟候选池AI排行榜（全新功能）

**需求**：每个交易日收盘前20分钟，本地AI对候选池中具备买入条件（STARTER及以上信号已出现、或WATCHING但技术面达标）的股票做横向打分排序，给出"最值得买/卖"排行与理由，用醒目通知推送，且需可在App内反复点开回看；该能力要能利用历史交易记录持续变聪明。

**现状**：`LocalAIAgent` 中最接近的能力是 `analyzeScreenResult`（选股当天触发一次，不是每日收盘前）与 `verifySignal`（单只股票信号复核，非候选池横向排名），均不满足需求。`TradeLessonManager`（复盘）与 `WisdomManager`（话术学习）已有的"从历史交易学习"能力可直接复用，不需要另起炉灶。

**需要新增**：
1. 新的定时触发点：`RealtimeMonitorService` 需在每个交易日14:40左右触发一次，且需要"今日是否已触发"标记防止tick间隔内重复触发。
2. 新AI方法（如 `rankCandidatesBeforeClose`）：把 `AIContextBuilder` 已有的胜率/历史盈亏数据，与每只候选股当前的规则引擎判断结果一起喂给AI，要求输出结构化排序结果（打分+理由），而非自由文本，便于解析展示。
3. 持久化：排行结果需要落地存储（新表或类似 `DecisionLogger` 的落盘方式），保证"可重复点击查看"。
4. 前端：`StockBridge` 需新暴露一个接口给前端展示排行榜；具体UI因A尚未读取 `assets/index.html`，本文档只定后端数据结构与接口，UI细节需要前端配合另行确认。

**优先级**：排在其他各项之后——R1-R9本质是在修正/完善已有的买卖判断逻辑，属于地基；这个排行榜建立在地基之上，地基未稳前先做意义不大。

**用户补充要求（已确认，实现时必须遵守）**：本地AI的排序/分析能力必须紧扣App自身的技术指标（分时均价、成交价、买入卖出价格等 `AIContextBuilder`／`TradingRuleEngine` 已经算出来的具体数字），不能笼统地拿"大盘/大环境"当主要依据去给出泛泛而谈、容易产生幻觉的结论。设计 `rankCandidatesBeforeClose` 的prompt时，喂给AI的应该是候选股各自的水线/VWAP/量比/形态数据和历史胜率这些App已经算好的具体值，让AI基于这些数字做判断和排序，而不是让AI自由发挥去评论宏观市场——这一点在R10动手实现前需要再深入设计一次prompt结构，不能简单套用`analyzeScreenResult`现成的写法。

---

### R11．日志重构（协调项，非A负责）

**现状（A读到的 `DecisionLogger`）**：按分类（monitor/decision/ai_analysis/operation/other）记录，14天保留。
**目标规格（用户描述，由另一位协作AI负责实现）**：每天新建一个文件夹，文件夹内每只股票一个独立日志文件，把该股票当天所有监控/AI分析/决策操作写入同一文件；保留7天，到期自动删除；每天全新写入，不追加旧文件。

**A的处理方式**：本节仅记录目标规格用于跨AI协调；A实现 R1-R8 时凡涉及 `DecisionLogger.get().logXxx(...)` 调用，一律沿用现有接口签名，不改动其内部结构；若 R10 需要新增日志类型（如排行榜结果），先使用现有分类占位，待日志重构完成后再迁移，迁移本身不在本次范围。

---

### R12．【新发现，2026-09-03】低开路径"昨日全天真实VWAP"缓存未持久化，App进程重启后必然清零（真正的bug，建议最优先修复）

**现象**（用户在正式开工前提出的问题）：低开候选股反复显示"正在异步获取昨日全天真实均价数据，本轮暂不评估底仓，下一轮tick自动重试"，且持续出现，不像设计预期那样"抓到一次以后一整天都不用再抓"。高开/平开候选股没有这个问题，候选池里能正常看到当日分时均价。

**根因（已直接通读 `RealtimeQuoteManager.java`／`TradingRuleEngine.java`／`WatchlistManager.java` 源码确认，非推测）**：两条路径依赖的是两种性质完全不同的数据：
- 高开/平开用的"当日VWAP"来自 `RealtimeQuoteManager.fetchMinuteLine()`，每次tick都无状态地现抓现算，不依赖任何跨tick保存的缓存，进程重启也不受影响。
- 低开用的"昨日全天真实VWAP"来自 `RealtimeQuoteManager.fetchPrevDayVwap()`——这个方法本身确实设计了3次重试+失败后用日K缓存兜底估算，理论上很难长期拿不到值。问题出在拿到值之后**存去哪了**：`WatchlistManager.savePrevDayVwap()`／`getPrevDayVwapIfMatches()` 用的是两个 `static` 内存 `ConcurrentHashMap`（`sPrevDayVwapCache`／`sPrevDayVwapDateCache`），代码注释自己也写着"不落库，重启App后自然清空"——但候选池里其他所有状态（止损位、峰值涨幅、形态日OHLC等）全部是写进 `watchlist.db` 这张真正的SQLite表的，唯独这一项例外，没有走同样的持久化路径。

Android后台监控服务被系统回收进程、重启是常态（尤其没加电池优化白名单时）。进程一旦重启，这两个内存Map就清零一次——不管当天之前有没有成功抓到过，下一次tick又要重新走一遍"异步获取"的第一轮（即用户看到的这条提示），在这次异步请求跑完之前，这只低开股票的底仓判断都会跳过。如果重启频率接近或高于两次tick的间隔，用户看到的就会是"一直在获取、一直没结果"的持续状态——与观察到的现象完全吻合。

**次要怀疑点（未能独立验证，仅供参考，不影响上面这条结论成立）**：`fetchPrevDayVwap` 用的"5日线"接口（`URL_TENCENT_DAY_QUERY`）是本项目唯一用到该接口的地方。A检索了一些第三方资料，只能确认这个接口地址本身真实存在、常被挪用算类似用途的数据，但没能找到足够权威的资料完整核实 `parsePrevDayVwap()` 里假设的JSON嵌套结构是否和腾讯真实返回完全一致，也无法从A的沙盒直接发请求验证（网络访问不到这个域名）。修完下面的持久化问题后，如果个别股票偶尔仍遇到"重试3次+日K兜底都失败"，可以查 `DecisionLogger` 里该股票的 `logPrevDayVwapFetch` 记录（成功/失败都会写一条，带具体原因字符串），那是最直接权威的证据来源，比继续猜测更可靠。

**建议修复方案**：把这两个内存Map改成 `watchlist` 表里的两个真实列（如 `prev_day_vwap REAL`、`prev_day_vwap_date TEXT`），写法完全比照现有的 `div_k_high`／`pattern_open` 等字段（`DbHelper` 升级到 `DB_VERSION=6`，读写走 `ContentValues`+`mDb.update`），这样无论进程重启多少次，只要还在同一交易日，缓存就不会丢。纯可靠性修复，不涉及任何阈值或判断逻辑改动，风险很低。

**状态**：可直接实现，性质与R8同级（不需要用户/审查AI拍板设计方向），但建议排在**第3节实施顺序最前面、先于R8**——因为R2低开路径统一设计能否验证出效果，前提是这个数据源本身得先能稳定拿到，不然后续对低开路径的任何调整都无法用真实数据验证。

---

## 3. 建议实施顺序（更新进度）

1. ~~**R12**~~✅已由B实现（低开VWAP缓存持久化修复）
2. ~~**R8**~~✅已由B实现（日志明细增强）
3. ~~**R1**~~✅代码中已实现（交易日历+年底提醒，认领人待补充）
4. ~~**R4**~~✅已由A实现（止损参照价口径，方案C）
5. ~~**R2 + R3**~~✅已实现（买入逻辑统一重构，D实现，“加仓”语义用户已确认选A提备选，代码已匹配）
6. ~~**R6 + R7**~~✅已由A实现（预警成本展示+T+1静默降噪）
7. **R9** 全程适用，不单独排期
8. **R10**（收盘前排行榜，新功能，排最后，见节内新增的AI训练原则）
9. **R11** 不由A实施，C持续实施中

---

## 4. 待确认问题清单（已全部确认，保留供追溯）

1. ✅R2(b)：按A方案确认（30分钟分界，底仓取`min`、加仓取`max`）。
2. ✅R2(c)：分工理解确认无误。
3. ✅R3(3)：确认不改全局节奏，用时间戳计时+可选局部快轮询。
4. ✅R4：选定**方案C**（两者取较严格）。
5. ✅R6：先上**方案A**（纯展示增强）。
6. ✅R7：确定"静默通知+角标"方案。

---

## 5. 变更记录（A）

- **2026-09-03　A**：创建本文档，完成需求梳理与技术方案初稿；直接读取并核对了当前全部相关源码；尚未开始任何代码修改，等待第4节问题确认后进入实现阶段。
- **2026-09-03　A**：针对用户开工前提出的"低开为什么一直获取不到昨日真实均价"问题，通读 `WatchlistManager.java` 定位到根因（新增 R12：内存缓存未持久化，进程重启即清零），已写入文档，建议作为最优先项，先于R8执行；本次未修改任何代码，仍在等待中。
- **2026-09-03　C（承接第2节R11，按B已有方案实施）**：R11「日志重构」由C接手实施（B已完成现状排查与方案设计，见 `B-决策日志系统联调修复-开发计划.md`；B目前在开发其他内容）。后端 `DecisionLogger.java` 已按目标规格完成核心重写（`decision_logs/日期/代码_名称.txt`，同一支股票的监控/决策/AI分析/操作按时间顺序混排进同一文件，保留7天自动清理，`logRulePush`／`logAiSupplement`／`logManualTrade`／`logPrevDayVwapFetch`／`logBuyLogicTrace`等对外方法签名保持不变，A后续实现R1-R10时可继续直接调用，无需改动调用方式）；正在修复`StockBridge.java`／`RealtimeMonitorService.java`与新结构的联调问题（即B文档2.1/2.2所述），并着手前端专属日志页面（对应B文档2.3）。进度同步记录在 `B-决策日志系统联调修复-开发计划.md` 的变更记录中。
- **2026-09-03　B**：R11确认由C接手后，B退出该项认领（原排查与方案见 `B-决策日志系统联调修复-开发计划.md`，已保留供C参考）；转而认领 **R12**（低开VWAP缓存持久化修复）并已实现：`WatchlistManager.java` 新增 `prev_day_vwap`／`prev_day_vwap_date` 两列（`DB_VERSION` 5→6），`getPrevDayVwapIfMatches`／`savePrevDayVwap` 方法签名不变、内部改为读写数据库，替代原来重启即清零的static内存Map。未做Gradle编译验证，详见 `B-VWAP缓存持久化修复-开发计划.md`。
- **2026-09-03　B**：接续认领 **R8**（日志明细增强），范围比A原文举例更广——`evaluateStarterGapUpOrFlat`／`evaluateAddHalf`／`evaluateFullPosition`（6道关卡此前全部空note）／`detectDivergenceKline`（此前完全无日志痕迹）均已补上判断用到的中间值或新增trace；`evaluate()`里满仓/加仓未触发时的具体原因也从"直接丢弃"改为拼进最终note。均只加日志明细，未改动任何触发条件或判断顺序，已写入`TradingRuleEngine.java`。未做Gradle编译验证，详见 `B-规则引擎日志明细增强-开发记录.md`。

- **2026-09-09　A**：Filesystem连接器本轮对话中多次出现超时（编辑/只读请求都遇到过），每次都先暂停并如实告知用户，等确认连接恢复后再重新读取确认真实状态，未凭猜测继续。过程中也碰到过一次真实的并发写写冲突：A先前对本文档的一批增量编辑成功写入后，再次读取时发现那批改动全部不见了，推测是另一位协作者在那之后用全量写入方式更新了整个文件（基于他们自己更早的本地副本），无意中覆盖掉了A的写入；重新写入后本轮未再出现这个问题。同一个共享文档被多AI并发编辑时，全量覆盖式写入会静默丢失其他人刚做的增量改动，建议后续AI协作编辑本文档时优先用增量编辑、避免全量重写。确认真实状态后：①核实了B对R12的实现（`WatchlistManager.java`新增`prev_day_vwap`/`prev_day_vwap_date`两列及配套读写方法），逻辑与预期一致，未发现问题；②核实了B对R8的实现（比A原方案范围更完整）；③核实了C对`DecisionLogger.java`的重写，确认`logPrevDayVwapFetch`等A/B已在用的方法签名保持不变，不影响现有调用；④正式实现了**R4**（止损参照价改为`max(形态日最低价, 前一交易日最低价)`，方案C），已应用到`evaluateStopLoss`，`state.prevYangLow`字段保留原有候选股清理用途未受影响；⑤把用户以"(S:...)"形式给出的全部确认意见，整理进了对应R条目的"状态"行以及第2/3/4节，并把R1的年底提醒需求、R10的"AI必须基于App自身技术指标而非大盘泛论"需求补充进了各自条目正文。本次未做Gradle编译验证，也未触碰`WatchlistManager.java`／`RealtimeMonitorService.java`／`DecisionLogger.java`（分别是B/C正在负责或刚完成的文件），避免和并行工作冲突。
- **2026-09-10　D**：读取全部相关源码后发现 R4（止损参照价方案C）、R8（日志明细增强）实际已在`TradingRuleEngine.java`中实现（R4改动注释日期为2026-09-08），但本节此前未记录对应变更、也未知具体实施者，已向用户提出，用户尚未回复，此事暂搁置，仅在此存档以免后续协作者误判进度。同时认领 **R2+R3**（底仓/加仓VWAP双参照统一设计＋高开平开重点监听计时确认）——第3节实施顺序中排在R4之后、当前最靠前的未认领项。已完整读取`TradingRuleEngine.java`／`TradingRuleConfig.java`／`WatchlistManager.java`／`RealtimeMonitorService.java`全文，形成详细实现方案，见 `D-VWAP双参照统一与重点监听确认-开发计划.md`（含两处D自行判断的解释性决定，已在该文档第4节标出，供用户核对）；本条记录写入时代码尚未开始修改。

- **2026-09-10　A**：读到D新建的`D-VWAP双参照统一与重点监听确认-开发计划.md`（认领R2+R3），方案整体扎实——`evaluateAddHalf`已有未用到的`prevDay`参数、`saveTrackState`落库时机、复用`isStopNotifyWindowMinutes()`而不另起一套窗口判断，这几处都是直接读代码验证出来的细节，不是猜测。对D在第4.1节主动提出、请用户核对的疑问，A想补充一个同样成立的备选理解：用户需求原文"1小时后可以购入底仓"与"收盘前15分钟...将进行加仓"用词并不相同（底仓 vs 加仓），字面上更像是两个先后独立的仓位动作——1小时确认成立先给底仓，如果这只股票能一路稳到收盘前15分钟仍不破位，再追加一次加仓，与本文档其它规则里"确定性越高、仓位越重"的整体思路也契合，而不只是同一个决定的两次尝试机会（D现在的理解）。这两种读法D的方案都能大致适配（D的读法改动更小，A的读法需要在满足收盘前窗口时改成走`ADD_HALF`而不是`BUY_STARTER`，且要求当时状态已经是STARTER），具体哪种更符合本意，还需要用户直接确认，A不代用户拍板。

- **2026-09-10　A**：根据用户指示，在R1/R6/R7中自选任务，优先考虑与其他AI并发风险低、优先级高的。先检查了R1：发现`TradingCalendar.java`已完整存在且三处集成点全部完成（详见R1节），性质跟R8一样是代码里已存在但未记录认领人，因此改选R6+R7。选择理由：R2+R3已被D认领；R6/R7与R1相比，虽然同样会碰`RealtimeMonitorService.java`，但经核实后D的R2+R3方案在该文件里只新增一个独立方法和`Action.NONE`分支里的小段判断，A的R6/R7改动集中在`applyT1Note`附近和通知渠道相关代码，两边文本位置不重叠，冲突风险低。已将R1状态改为已实现，并为R6（新增`applyCostBasisNote`）、R7（新增静默通知渠道+`fireQuietAlert`）写入了具体实现位置方案，均只需改`RealtimeMonitorService.java`一个文件、不碰`TradingRuleEngine.java`。本条记录写入时代码尚未修改，按用户要求的"先认领再改代码"顺序，接下来就实现。

- **2026-09-10　A**：完成R6+R7实现。`RealtimeMonitorService.java` 新增 `applyCostBasisNote`（R6，在`applyT1Note`后调用，仅对`WARN_PRESSURE`生效）、`CHANNEL_ID_QUIET`+`fireQuietAlert`（R7，`evaluateAndAct()`中根据`getSellableQuantity<=0`分流）。均只改了这一个文件，未碰`TradingRuleEngine.java`/`WatchlistManager.java`/`DecisionLogger.java`，保持与B/C/D并行工作无冲突。已将文档里R6/R7的状态行、第3节实施顺序、顶部总状态同步更新为已实现。尚未做Gradle编译验证，建议用户或审查AI有机会时实际编译一次。至此R1/R4/R6/R7/R8/R11/R12均已完成或进行中，剩余R2/R3（D认领，待用户确认4.1节）、R10（未认领）。

- **2026-09-10　D**：谢谢A的审阅。R2+R3代码已实现完成，改动`TradingRuleConfig.java`/`WatchlistManager.java`/`TradingRuleEngine.java`/`RealtimeMonitorService.java`四个文件，具体改动内容已写入R2/R3节状态行及 `D-VWAP双参照统一与重点监听确认-开发计划.md`。关于A提出的备选读法（先`BUY_STARTER`后`ADD_HALF`两次先后动作）：认同A说的这个读法同样成立且与本项目整体思路更契合，但D没有改回去——一是因为这确实是个需要用户拍板的分歧点，不应由任何一个AI单方面改动已实现的代码；二是因为`focusWatchStatus`字段本身只有NONE/WATCHING/CONFIRMED三态（A文档R3节原始设计即如此），若改成A的读法需要额外区分"1小时确认后的STARTER"与"收盘前确认后的ADD_HALF"两种后续，改动面比D现有实现大，值得用户确认后再一次性改到位，而不是先改一次否则可能还得改回来。已在R3节状态行明确标出这个待确认项，等用户直接回复。

- **2026-09-10　B**：看到A在R1节提的问题——R1确实是B实现的。重新连上会话后发现实际时间已从之前以为的09-03过去了好几天，期间A/C/D都在继续推进，本次只补一条归属记录，不重复确认/不写全量覆盖，避免重复触发A提到的并发覆盖问题。R1（`TradingCalendar.java`+`assets/trading_calendar.json`+`StockMasterApp`/`MainActivity`/`MarketDataManager`/`RealtimeMonitorService`四处集成点，具体改动见R1节）确认由B实现，与A核实的内容一致。另外R4遇到两次Filesystem MCP超时，均已重读确认未造成半截/损坏写入才继续。

- **2026-09-10　A**：用户对R3“加仓”语义直接拍板：选定A之前提出的备选读法（先底仓后加仓两次先后动作），并要求记录并通知D按此修改。A重新读取`TradingRuleEngine.java`/`TradingRuleConfig.java`现实现后发现代码已经正确匹配用户选定的读法（注释里明确写着“用户已拍板选择先底仓后加仓读法”，`focusWatchGraduation`新分支已存在）——推测D已在A本轮编辑这个文档之前就完成了同步修改，本条只是确认并把R2/R3状态行、第3节、顶部总状态同步收束，未重复修改代码。至此R1/R2/R3/R4/R6/R7/R8/R11/R12均已完成或进行中，仅剩R10（未认领）。根据用户“继续完成剩余任务”的指示，A接下来认领R10。

- **2026-09-10　A**：完成R10实现。至此开发计划里的R1至R12全部已完成或持续推进（R11仍由C负责中）。本轮过程中一并发现并修正了一处自己的小失误：在确认R2状态时把`evaluateStarterGapDown`误打成了`evaluateStarterGapUpOrFlat`，发现后立即改回来了（两者分别对应低开/高开两条不同路径，不能混用）。另外过程中写代码注释时出现了几处小打字错误（如“跿erifySignal”少了个ve），尝试修复时因错误信息每次回显不一致没能修正，不影响编译和功能，先跳过，后续有机会可以再清理。本轮共涉及4个文件：`LocalAIAgent.java`（新增R10两个方法）、`RealtimeMonitorService.java`（R10触发/快照/通知，发现还包含了D对R2/R3语义修正后的新代码与我自己的R6/R7两者并存无冲突）、`DatabaseManager.java`（新增字段+两个方法，故意绕开GreenDAO）、`StockBridge.java`（新增一个接口）。自始至终没有去碰`WatchlistManager.java`（D刚改完）或`DecisionLogger.java`（C负责中）。整个项目到目前为止仍然没有任何人实际跑过Gradle编译，这是目前最大的遗留风险，建议作为下一步优先事项。

- **2026-09-10　A**：认领R10，重新读了 `AIContextBuilder.java`（确认现有“事实类数字全部Java端算好，AI只负责解读，不自己编数字”这条设计原则早已存在于现有代码中，与用户对R10的要求完全一致，可直接沿用这个模式）、`LocalAIAgent.java`（verifySignal/analyzeScreenResult的prompt构建模式）、`WisdomManager.java`。具体方案见下方新增R10实现节。

### R10实现方案（A，2026-09-10）

1. **新增 `AIContextBuilder.buildCandidatePoolSnapshot()`**：遍历当前候选池中状态为STARTER/ADDED/FULL（已持仓）或WATCHING但技术面达标（有focusWatchStatus进展或已触发过PENDING）的股票，每支拼出具体数字：现价、水线、当日VWAP、量比、当前状态/动作标签（来自TradingRuleEngine.RuleResult）、近期胜率（buildStockHistoryNote）、近涨跌幅（buildRecentTrend）。不引入任何大盘/宏观数据——这正是用户要求的“只紧扣App自身技术指标”。
2. **新增 `LocalAIAgent.rankCandidatesBeforeClose(List<Snapshot>, callback)`**：prompt结构仿 `verifySignal` 的“先推理再结论”格式，但额外硬性要求：“只能引用下面列出的具体数字作为理由，不得提及大盘/宏观经济等未列出的信息，每支股票的评分理由必须至少引用一个下面给出的具体数值”——直接对应用户“不要笼统参考大环境给出幻觉回复”的要求。输出解析为结构化排名（股票代码+评分+理由）。注入WisdomManager的GENERAL分类话术（与verifySignal用法一致）。
3. **触发时机**：`RealtimeMonitorService`在交易日tick循环里新增判断：当前时间进入[14:40,收盘)且今天还没触发过（日期字符串标记，跨日自然重置，同样模式已用在R1的needsYearEndReminder等处），调用rankCandidatesBeforeClose。
4. **持久化**：新增 `DatabaseManager` 一张轻量表 `candidate_ranking(date TEXT PRIMARY KEY, result_json TEXT, created_at INTEGER)`，一天最多一条，跨日自然新增不覆盖旧记录（满足“可重复点击查看”）。
5. **通知**：复用现有`fireAlert`高优先级通道，标题固定“今日候选池排行榜已生成”。
6. **StockBridge接口**：新增 `getCandidateRanking(date)` 返回存储的JSON，供前端后续展示用——本轮只做后端接口，具体展示页面不改`assets/index.html`，做法与 R7 角标部分一致——后端数据就绪，前端展示另行确认。

**注**：第2步的prompt具体措辞是A的设计判断，不是需要用户事先拍板的开放问题（同 R6/R7 的通知文案一样属于A自主设计范围），但因为用户特别强调过这部分要“深度思考”，建议实现后用户或审查AI实际看一下生成的prompt模板文本。

**状态**：✅已全部实现（A，2026-09-10）。六个步骤均已落地：`LocalAIAgent.rankCandidatesBeforeClose`+`buildRankPrompt`；`RealtimeMonitorService.maybeFireCandidateRanking`（14:40-14:41窗口触发，日期字符串去重）+`buildCandidateSnapshotText`（取现有`lastNote`字段，不重新发请求/跑规则引擎）+`fireRankingReadyNotification`；持久化改用`DatabaseManager`新增的独立SharedPreferences（`saveCandidateRanking`/`getCandidateRanking`），**没用GreenDAO新建实体**——新增GreenDAO实体需要annotation processor重新生成XxxDao代码，这一步只有实际编译才会发生，当前协作环境没人能验证，直接加实体类会有编译不过的风险，改用SharedPreferences彻底避开。`StockBridge.getCandidateRanking(date)`提供前端读取接口，具体展示页面本轮不做。未做Gradle编译验证。

- 2026-09-14 A: 根据用户指令修改了TradingRuleEngine.java的四处逻辑（取消所有买入路径的放量确认、取消重点监听中途取消、独立破位参照价改为单一的前交易日最低价），并在WatchlistManager/RealtimeMonitorService/assets index.html中新增了前交易日最低价（止损价）的UI醒目展示。未做Gradle编译验证。

---

## 附录：用户需求原文（完整保留，供追溯）

> 放量，中间有缩量，第一种仙人指路一定要放量
>
> 以下规则说明的日期，均是大盘交易内的日期，排除法定节假日，周六周日等不开盘日期，所以在记录分时均价日期的，收盘价日期的时候，一定要重点注意。
>
> 买入规则：第二天的价格要大于选出这只股票当天的分时均价，比如，我是27号筛选出了这只股票，28号开盘买入的时机是实时价格大于27号的分时均价。
> 底仓的买入条件：如果这只股票低开，那只能当这只股票站到当天（28号）的分时均价，才能建一些底仓。这里有一个问题，现有代码是如何判断当天的分时均价的？是盘中么？正常的电脑端炒股软件是能够看到分时均价图的，能够看出当天买入的平均成本价格是多少，但是咱们这个只能是默认计算（或者有什么办法可以获取到分时均价图的数据）
>
> 低开的加仓条件：当价格如果站上27号分时均价，再进行加仓。这期间，如果价格来回反复震荡，或者上升了很高（不追），只有回跌到分时均价，再进行补仓
>
> 这里会不会有一个误区，就是28号的分时均价和27号的分时均价，价格两者互斥，导致一致买入不了（这里是我的疑问点，需要你回复，当前代码的判断和计算逻辑，还有如果要实现的判断计算逻辑），所以这里要动态计算一下，比如28号的实时价格只要站上27，28号的分时均价就可以进行底仓，只要再大于另一个分时均价就可以加仓。因为刚开盘，今天的分时均价没有出来，但是昨天的分时均价是固定的。（相当于今天的分时均价在盘中/收盘的时候，才是很重要的判断指标）
>
> 高开/平开买入：不能盲目追涨，当价格回落到当天分时均价附近的时候，进行提醒可以留意进行底仓买入。如果来回在分时均价上下浮动，就可以购买，如果完全跌破是不可以购买的。这里就很考究，比如10分钟前价格分时均价为8元，过了5分钟，价格站上了分时均价，又过了5分钟，价格又跌下去分时均价。那这时候是买入还是不买呢？如何设计这个动态的变化，不让买家被套，又不让买家晚入场很关键。但是，其实我在想，如果晚买入 也就是节省了一部分钱，总比今天买完，t+1规则买不了，跌大部分钱好。所以，我决定，当出现高开，并且回落到分时均价线，进行推送提示。该股票进行着重监听，原本的监听时间进行缩短一半，并进行计时，大盘交易时间的1小时后（中午休盘不算在1小时内）如果还是能够稳定在分时均价，可以购入底仓，在大盘收盘前15分钟，如果不跌破分时均价，将进行加仓。
>
> 清仓卖出逻辑：今天价格（28号的价格）跌破昨天（27号的价格）的最低价格，全部卖出，一点不留。（昨天和今天都是实际大盘交易日期）
> 注意：并不是盘中的时候今天的价格打到了昨天的最低价格，就直接提示卖出，而是当临近收盘（15分钟）的时候，价格还是收不上来昨天的最低价格（收盘价格一直收不上来，注意只有收不上来，只要不跌破就不给卖出信号），这个时候，一定要提示，全部卖出止损！！！
>
> 当这只股票涨幅5%，当回落到2.5%也就是一半的时候，需要注意。
>
> 并且还有一个重要事项，这个看在当前代码逻辑中应该如何适配而不产生bug。就是当我买入的票，由于大盘或者其他特殊原因，一直在跌，并且收不回当天的分时均价，并且因为t+1规则，只能明天卖出，第二天只要是涨会到我的买入价格（可能达不到我的买入价格，而是达到昨天的分时均价价格，这个时候也是可以卖出的，亏点比套住好），就直接止损卖出出来。但是这个可能和原有的买入/卖出逻辑互斥，所以要很谨慎的考虑这个逻辑。可以先梳理出正常的买入/卖出逻辑，看什么情况下执行这种特殊注意事项判断。而且再有一个问题，当这只股票第二天要是收了阴线，并且一直涨呢，所以，最终的结论应该是当第二天收盘的时候，还收不上来昨天的最低价格（非昨天的分时均价而是昨天的最低价，也就是卖出的逻辑警戒线），无条件离场止损卖出。
>
> 现在很大程度上都没有再加仓的提醒，一直都是提醒我减仓。我理解的是，当天完成，第二天高开的话，会走高开的规则判断，并且前一天的分时均价也都更新，会有回踩分时均价的时候，一定会有提醒加仓的机会，但是现在整个程序，给我的感觉就是死气沉沉，完全就是只要一盈利就要跑，我再之前可能一直强调的时候右侧交易，落袋为安，但是实际上，只要遵守上述交易规则，盈利是完全没问题的。并不是一直看衰的，并且模拟软件如果不大胆一点，真实交易场景下只会更加畏手畏脚，所以要严格按照上述的操盘手规则进行适配修改，并不是你主观上加入的条件限制，如果真再测试过程中，咱们这套逻辑太过于激进，我会适当修改。
>
> 其他关键规则笼统记录：产生分歧？分歧转一致？有分歧就会放量，放量需要看多方赢还是空方赢，如果多方赢 一定会盈利。
>
> 很明显的回踩，但是还是继续观察　五洲交通(600368) 现价¥4.38｜【高开/平开路径】今开大于等于昨收¥4.34，现价¥4.38 VWAP¥4.38，尚未回踩确认到分时均价附近，继续观察
>
> 我明白现在的提醒逻辑了，比如我刚才买入的万安科技，买入的时候今天整体涨幅在1.98，但是买完以后回落到0.5左右，这时候就会触发减仓提醒，也就是我每次买入的时候，都不是追适合的买入价格，可能会贵几毛，导致每次买入以后，都会提示我进行减仓。减仓这个判断需要优化，当天买入的怎么也需要t+1以后才能卖出，所以当天的不进行卖出提示，只有真正能够卖出的股票才会有卖出减仓提示。买入这里应该是我主动买入导致的，这个我接受，但是我还是要对当前代码的操盘手规则判断进行优化修改，就像我之前说的，模拟的时候都不敢操作，真实交易更不敢了。（但是也不能偏离操盘手规则，只是之前的vmap，放量等限制条件尽可能的放宽，以适配当前情况）需要明确一个点，股票不是一直升，也不是一直降，是来回起伏的，可能围绕一个价格来回反复横跳，所以需要深度思考这块需要怎么设计。
>
> 还有一个问题，我们现在已经放宽了一些条件，盈利和亏损的几率都提高了，但是股票这种益智，技术，胆量于一身的游戏，肯定是要最大程度提高盈利的。所以，现在就需要你深度思考一下，如何让本地ai大脑更聪明的适配我们这套逻辑。目前是筛选出来的股票会有很多，有的有可能盈利，有的有可能亏损，在这种前提下，我们的资金还是有限的，如何能更好的，更准确的预判哪只股票会盈利，就很重要了。我的想法是，假如筛选出来了3只很适合买入的股票，本地ai会根据历史的操盘记录去分析，哪只股票评分更适合买入，并列出对应的原因和评分。更适合买入，需要根据以往的股票信息操作，根据股票的技术指标，价格，买入卖出等判断逻辑等等，这一块需要你深度思考应该如何让本地ai更懂当前代码。目的是做出筛选出来可买入的股票，再次评审出最适合的，这个操作要在收盘前20分钟完成，并给出具体的提醒。（因为这种情况下，能够准确确定要买入的股票，需要看整天的分时均价，和是否能站稳对应的价格，以上是基于我真实进入市场需要的功能，先行在模拟软件中进行开发的一个功能）确保资金的合理运用，再真正进入市场之前扩大买入卖出等交易限制（但是不脱离整体的操盘手限制规则），并通过不断的经验判断，让本地ai和运行日志调试更加理解规则，更加适配规则，更容易选出优质股票，从而再最终进入真实市场的时候，能够有更好的应对风险的能力。所以我想的是，模拟交易的时候，可以正常买入/卖出提醒，但是这个功能，相当于全天一直在监听候选池股票信息，当临近收盘20分钟的时候，将这个优质股票买入/卖出排行榜列出来，（这个通知提醒弹框要醒目，进入app以后要有留痕，并且可以重复点击查看）最重要的是，这个功能要让本地ai学习并理解，这是一个长期学习进步的本地ai功能，这个很主要，需要怎么训练本地ai读取并学习关键数据，让它能够更好的适配规则，选出最优质的股票，而不是判断大盘等信息给我们另一种消极的非操盘手规则结论，，因为我们做的是短线右侧交易，所以资金的正确合理使用，才会让盈利增加，所以这一步至关重要。
>
> ✅ 重新修改日志写入内容和格式，更加精准适配规则和操作规范。日志改成每日新生成一个文件夹，每只股票生成一个单独的日志信息，将对这只股票的所有监控，ai分析，决策操作等日志，全都写到对应股票的对应日志文件中，所有当天的股票放在同一个文件夹中。决策日志点击以后跳转到一个新页面，用于展示每天的股票日志信息，所有日志信息都保留7天，7天后自动删除。每天的股票信息日志都是新写入，不操作旧日志文件。
