当前项目是基于前端页面展示，java代码交互的操盘app。你需要仔细查看当前前端页面中的持仓和筛选出来的股票的实时监控逻辑，确定好当前校验条件和逻辑判断。然后，仔细阅读根目录的操盘手经验终版.md文件，了解规则和注意事项。规划原有的实时监控要如何修改，将修改方案列出，其中的规则和注意事项按照操盘手经验终版.md文件中进行。

一、当前实时监控架构（现状梳理）
1.1 整体链路
监控逻辑全部在 Java 层，前端只负责展示与人工确认，不参与规则计算。

WATCHING
STARTER/ADDED
命中
选股页 启动监控
RealtimeMonitorService
同步持仓到候选池
批量拉实时行情
状态?
拉分时数据
仅行情
TradingRuleEngine
LocalAIAgent 二次验证
markPending 待确认
AI确认则推送通知
前端候选池展示 + 用户确认/忽略
层级	职责
前端 index.html
启动/停止监控、展示候选池状态、待确认信号、决策日志；持仓页仅刷新现价
RealtimeMonitorService
30/60 秒轮询，持仓同步，规则初筛，AI 复核，通知
TradingRuleEngine
确定性数值规则（Layer 1）
LocalAIAgent
规则命中后的二次验证（Layer 2）
WatchlistManager
状态机 + 待确认队列
1.2 监控覆盖范围
候选池：选股入池后 WATCHING 状态的股票
真实持仓：每轮开始时从 Position 表同步进候选池，自动标记为 STARTER，使止损/加仓规则生效
持仓页：无独立监控逻辑，只通过 onPriceUpdate 刷新盈亏展示
1.3 当前三条硬规则（TradingRuleEngine）
规则 1：买入底仓（WATCHING → BUY_STARTER）

条件	当前实现
前提
有前一日参考价，且数据未陈旧
条件 A
今开 < 前收（必须低开）
条件 B
现价 ≥ 今日分时最低点 × (1 + 0.3%)
条件 C
最近 5 个分时点未再创新低
数据依赖
需拉取分时数据（fetchMinuteLine）
规则 2：加仓（STARTER → ADD_POSITION）

条件	当前实现
触发
现价 > 前一交易日收盘价（突破水线）
说明
与文档「突破水线加仓」概念接近，但未区分底仓/加仓/满仓三档
规则 3：止损（STARTER/ADDED → STOP_LOSS）

条件	当前实现
优先级
最高，先于加仓判断
触发
现价 < 前一交易日最低价
说明
静态参照，非动态「分歧 K 线」
安全阀（已有，应保留）

前一日 K 线日期与预期最近交易日比对，陈旧则拒绝产生任何信号
待确认状态（PENDING_*）跳过本轮评估，防刷屏
信号落地为「待确认」，不自动买卖；AI 不确认则静默记录、不推送
当前硬编码参数

STABILIZE_BUFFER_PCT = 0.3%      // 站稳分时低点缓冲
STABILIZE_LOOKBACK_POINTS = 5    // 止跌确认点数
STARTER_POSITION_PCT = 0.3       // 底仓建议比例（仅文案）
ADD_POSITION_PCT = 0.3           // 加仓建议比例（仅文案）
1.4 前端展示逻辑
候选池状态：WATCHING / STARTER / ADDED / PENDING_*
每轮 tick 刷新 lastNote（规则引擎观察说明）
待确认信号展示 AI 认可/存疑、触发价、确认/忽略按钮
缺失：无 VWAP、量比、止损位、T+1 可卖状态、收盘前 30 分钟等特殊展示
1.5 AI 二次验证（Layer 2）
规则命中后调用 LocalAIAgent.verifySignal()
Prompt 含：规则依据 + 实时行情 + 近期走势 + 历史交易 + 已学话术
AI 输出「确认/不确认」+ 大白话说明
缺失：未注入 VWAP、量比、分歧 K 线位、T+1 状态等关键上下文
二、与《操盘手经验终版.md》的核心差距
文档第九部分已做对比，归纳如下：

场景	当前 App	文档要求	差距性质
候选买底仓
低开 + 站稳分时最低点
水下 + 站上分时均价（VWAP）
判断依据完全不同
放量验证
完全没有
贯穿全文的核心机制
最大缺口
加仓
突破前收（单一档位）
底仓/加仓/满仓三档 + 上影线回补
缺档位拆分
止损
跌破前一日最低价
分歧 K 线中点/最低点 + 前阳最低价
静态 vs 动态
通知时机
命中即推（AI 确认后）
止损类需收盘前 30 分钟
时机规则缺失
T+1
未追踪
区分可卖/不可卖仓位
工程约束缺失
时间窗口
无
开盘 60 分钟/收盘前 20 分钟提高放量门槛
进阶能力缺失
文档明确：当前 3 条规则是早期粗略近似，不是完整版本；「同步」不是调数字，而是换掉判断依据本身。

三、修改方案（按文档 9.6 落地优先级）
阶段 0：保留现有架构（不动）
以下设计符合文档 9.2，继续保留：

两层架构：Layer 1 规则引擎 + Layer 2 AI 复核
人工确认机制（待确认 → 用户点确认）
陈旧数据安全拦截
决策日志
持仓自动同步进候选池
阶段 1：全规则加放量检查（优先级最高，改动最小） ✅已完成
目标：对齐文档 3.1 / 6.3「放量验证分歧」核心原则。

改动点

TradingRuleEngine 新增量比计算模块：

当日累计量 / N 日均量（默认 N=5，阈值 1.8×，标注为建议值）
最近 5 分钟量 vs 当日均量 或 vs 历史同期均量（6.3 双口径，任一口径达标即可）
三条现有规则均增加放量门槛：

买底仓：站上 VWAP 后还需量比达标（缩量突破降级为观察，非直接否决）
加仓/突破水线：需放量确认
止损/破位：放量跌破 → 全仓卖出；缩量跌破 → 一级预警观察（6.3 卖点 10 分钟窗口）
RealtimeQuoteManager：确保 Quote.volume / MinutePoint.volume 可用于量比计算；需从日 K 缓存取 N 日均量。

前端：候选池 lastNote 展示量比数值；待确认信号标注「放量/缩量」。

参数外部化（文档 9.4）：量比倍数、观察窗口分钟数 → JSON 配置文件，不写死。

阶段 2：买入判断整体替换（水线 + VWAP 矩阵） ✅已完成
目标：对齐文档 1.3 / 3.2 买入状态机。

替换现有规则 1，改为：

状态	条件	动作
观察中
水下 + 分时均价下方
不可买，累计观察天数
底仓可建
水下 + 站上分时均价（持续 N 分钟确认，建议 3~5 分钟）
BUY_STARTER
加仓可建
突破水线（= 前收）；或底仓后回踩 VWAP 不破
ADD_POSITION
满仓可建
现价回补上影线长度 ≥ 70%（建议值）
BUY_FULL（新动作）
技术实现

VWAP 计算：用分时数据滚动计算 累计成交额 / 累计成交量；腾讯分时已有 avgPrice 字段，需验证口径是否为 VWAP，不一致则自算。

删除/废弃：

「必须低开」前置条件（文档：水下即可，不要求低开）
「站稳分时最低点 0.3% + 5 点不创新低」
WatchlistManager 扩展：

新增状态字段：watch_days（观察天数）、vwap_above_since（站上 VWAP 起始时间）
新增动作：BUY_FULL → PENDING_FULL → 确认后 ADDED（或独立 FULL 状态）
前端：

候选池展示：现价 vs 水线、现价 vs VWAP、观察天数
状态标签增加「底仓可建 / 加仓可建 / 满仓可建 / 待确认·满仓」
需你确认的口径（文档 1.6 vs 1.3 张力）：1.3 说水下+站上 VWAP 可打底仓；1.6 通知第 1 条把「站上水线」和「建底仓」放一起。建议按 1.3/3.2 实现：底仓 = 水下+站上 VWAP；突破水线 = 加仓。

阶段 3：止损升级为三级体系 + 分歧 K 线追踪 ✅已完成
目标：对齐文档 1.4 / 3.3。

替换现有规则 3，改为：

级别	触发条件	动作
一级预警
当日涨幅回撤至峰值涨幅 50%
提示关注，非强制卖
二级触发
跌破分歧 K 线中点 (H+L)/2
全仓卖出
三级兜底
跌破分歧 K 线最低点
必须清仓
独立破位
跌破前一根阳线最低价
立即止损（无论上述状态）
新增状态追踪（WatchlistManager / 新表）

divergence_kline: { date, open, high, low, close, mid, isActive }
prev_yang_low: 前一根阳线最低价
peak_gain_pct: 当日峰值涨幅（用于一级预警）
分歧 K 线识别（持仓期间动态更新）：

放量（量比达标）+ 滞涨特征（十字星/长上影/涨幅回撤）
新分歧出现时更新参照位（移动止盈，文档 4.4）
通知时机（文档 1.6 第 3 条）：

二级/三级止损信号 → 仅在收盘前 30 分钟内推送（14:30 后）
一级预警 / 独立破位 → 可盘中即时推送
前端：

持仓/候选池展示当前止损位（中点/最低点/前阳低）
待确认止损标注「距收盘 X 分钟 / 可执行卖出 / T+1 不可卖」
阶段 4：买入拆为三档 + 通知规则对齐 ✅已完成
目标：对齐文档 3.2 / 3.4 状态机与 3 条通知。

通知	触发状态	当前	改后
1
底仓可建
无对应
进入 底仓可建 推送
2
满仓可建（分歧转一致）
混在「加仓」里
独立 BUY_FULL 推送
3
即将触发二级/三级止损
即时推送
收盘前 30 分钟推送
WatchlistManager 状态机扩展：

WATCHING → PENDING_STARTER → STARTER
STARTER  → PENDING_ADD      → ADDED
ADDED    → PENDING_FULL     → FULL（或 ADDED+标记）
任意持仓 → PENDING_STOP     → STOPPED
前端：三种待确认样式区分（底仓/加仓/满仓/止损），止损类显示倒计时。

阶段 5：T+1 可卖状态 + 涨跌停检测 ✅已完成（2026-07-25）
目标：对齐文档 5.4 / 8.5。

Position / TradeRecord 每笔买入记录 buy_date
卖出信号触发时计算：
可执行卖出：该笔已过 T+1
信号已触发但当日不可卖：提示最早可平仓日期
涨跌停检测：触及涨跌停且无对手盘 → 信号标记「顺延至下一交易日」
同日保护（文档 5.3 建议）：底仓+满仓同日触发 → 默认推迟满仓至次日，或通知标注「今日买入 T+1 前不可卖」
前端：待确认止损/卖出信号展示「可卖股数 / 锁定股数」。

阶段 6：参数全部外部化 ✅已完成
目标：对齐文档 9.4 / 8.3。

新建 trading_rules.json（或类似），包含：

{
  "volumeRatioThreshold": 1.8,
  "volumeMaDays": 5,
  "vwapConfirmMinutes": 5,
  "shadowEatRatio": 0.70,
  "stopNotifyMinutesBeforeClose": 30,
  "earlyWindowVolumeMultiplier": 1.5,
  "fullConfirmMinutes": 45,
  "sellObserveMinutes": 10
}
TradingRuleEngine 启动时加载，回测更新参数无需发版。

阶段 7：AI 层适配（Layer 2） 🟡大部分完成
目标：对齐文档 9.7。

verifySignal Prompt 增强：注入 VWAP 位置、量比、分歧 K 线位、T+1 状态、当前买入档位
按判断类型注入话术（非全量）：买底仓 / 满仓确认 / 止损 / 早盘诱多识别 分类注入
AI 职责边界：
做：分歧转一致可信度、早盘诱多/尾盘拉高模式、形态二额外审查
不做：数值计算、仓位数字决策、走势预测
反馈机制（文档 9.5）：定期回顾 AI「不确认」样本，高频原因下沉为 Layer 1 硬规则
阶段 8：进阶能力（验证后再上） ⏳按计划暂不做
文档建议放在前 6 步落地之后，且需回测验证：

能力	文档章节	说明
正 T 机制
6.1
先买后卖等量老仓，撤销当日加仓过夜风险
时间窗口放量分级
6.2
开盘 60 分钟/收盘前 20 分钟提高门槛；满仓渐进确认 30~60 分钟
大盘过滤器
4.5
指数跌破关键均线时暂停满仓信号
ATR 止损缓冲
4.2
收盘价跌破 + 0.3×ATR 才确认
形态二识别
七
推测内容，先观察池/回测，不实盘
移动止盈
4.4
新高分歧 K 线上移止损位
四、前端需配合的改动清单
模块	改动
候选池卡片
展示水线/VWAP/量比/止损位/观察天数/T+1 状态
状态标签
新增 PENDING_FULL、一级预警、待确认·满仓
监控面板
收盘前 30 分钟倒计时提示；参数配置入口（可选）
持仓页
展示关联监控状态（来自 watchlist 同步项）；非独立规则
决策日志
记录新字段：量比、VWAP、分歧 K 线位、止损级别
通知文案
区分底仓/加仓/满仓/止损级别；T+1 不可卖警告
五、建议实施顺序与工作量估计
第1步  放量检查（1~2天）          ← 价值最高、风险最低
第2步  水线+VWAP 替换买入（2~3天） ← 核心逻辑变更
第3步  分歧K线+三级止损（3~4天）   ← 需新状态追踪
第4步  三档买入+通知时机（1~2天）
第5步  T+1+涨跌停（2天）
第6步  参数外部化（1天）
第7步  AI Prompt 适配（1天）
第8步  正T/时间窗口/形态二（按需）
六、关键决策点（实施前需你确认）
底仓通知口径：按 1.3（水下+站上 VWAP）还是 1.6（站上水线+底仓）？
同日满仓保护：底仓+满仓同日触发时，是否默认推迟满仓至次日（文档 5.3 建议）？
分歧 K 线中点算法：用 K 线自身 (H+L)/2，还是相对前收涨幅回撤 50%（文档 3.3 两种都出现过）——建议两种都实现、可切换。
形态二：暂不实现，仅观察池（文档 3.1 / 7.4 建议）。
如果你认可这个方案，我可以从**阶段 1（放量检查）+ 阶段 2（VWAP 买入替换）**开始写具体 Java 改动清单和接口设计，或者直接动手改 TradingRuleEngine。你希望先确认上面 4 个决策点，还是按文档建议的默认口径直接开干？

按 1.3（水下+站上 VWAP）有风险但是收益更大是么？如果是，按照这个来做。如果当日能够达到满仓的条件，可以当日满仓，但是一定是技术指标和所有规则都满足，才能给我提示，可以满仓。分歧 K 线中点算法两种都实现、可切换。形态二：暂不实现，仅观察池。一定是按照文档要求去修改规则和监控条件，并且监控提醒一定要准确，比如 太极集团建议底仓/建议增加50%仓位/建议满仓！/建议抛压/建议立即清仓止损等，提示信息一定要明确，并且是规则校验和ai复核都通过，再进行提示，并且当出现这种触发规则的时候，一定要把日志记录清楚（包括ai 的决策思考内容），方便后续跟踪确认，是否符合规则逻辑

按 1.3（水下 + 站上 VWAP）推进：风险更高、潜在收益也更大；当日若全部指标与规则满足，可当日满仓。开始按文档实现规则引擎、监控与明确提示，并仅在规则与 AI 复核均通过时通知且完整记录日志。

七、实施进度记录（持续维护，每完成一阶段更新一次）

【总体状态】2026-07-25：阶段 0-4、6 已完成；阶段 5 完成；阶段 7 基本完成（按判断类型注入话术已实现，仅剩AI"不确认"样本定期复盘这一运营流程未做）；阶段 8 按计划暂不做。

### 阶段 1-4、6 —— 复核确认已完成（早前会话完成，本次仅做代码复核）

2026-07-25 复盘代码库时确认：TradingRuleEngine / WatchlistManager / RealtimeMonitorService /
DecisionLogger / TradingRuleConfig 已经把放量检查、水线+VWAP买入矩阵、三档买入状态机
（BUY_STARTER/ADD_HALF/BUY_FULL）、分歧K线三级止损（LEVEL1_WARN/LEVEL2_MID/LEVEL3_LOW/
YANG_BREAK）、参数外部化（trading_rules.json）、规则+AI双通过才通知、完整决策日志
（含AI原始推理文本）全部实现，且与本文档口径一致（含六、关键决策点里确认的1.3口径、
分歧K线中点双算法可切换、当日满仓允许但需全部条件满足）。未逐项重新验收细节，
后续如发现偏差再补记录。

### 阶段 5：T+1可卖状态 + 涨跌停检测 —— 已完成（2026-07-25）

改动文件：
- TradingRuleConfig.java / trading_rules.json：新增 mainboardLimitPct(0.10) /
  gemStarLimitPct(0.20) / bjExchangeLimitPct(0.30) 涨跌停幅度配置
- TradingRuleEngine.java：
  - 新增 LimitInfo（涨跌停价 + 疑似封板检测）与 computeLimitInfo()
  - 封板检测用两个启发式代理信号：①一字板特征（开=高=低=现价）②近期分钟成交量
    相对当日峰值分钟量萎缩<5%。本地未接入真实买一/卖一挂单量，不保证100%准确，
    仅作提示性参考，文案中已注明“疑似”
  - annotateLimitAndHoldingPeriod()：买入类信号统一追加“T+1前不可卖”提醒；
    卖出类信号触及跌停且疑似封板时追加“可能无法成交，将顺延至下一交易日”提醒
  - 顺手修复一个bug：WATCHING状态分支之前没有把state存回stateUpdate，
    导致观察期累计的峰值涨幅在转入底仓后会从零重算
- DatabaseManager.java：
  - getTodayBoughtQuantity/getSellableQuantity/getLockedQuantity
  - insertTrade的SELL分支新增T+1硬性拦截：卖出数量超过可卖数量时返回-2且不写入
    任何数据（区别于资金不足的-1）
  - getPositionsJson()暴露sellableQty/lockedQty
- StockBridge.java：暴露getSellableQuantity(code)供前端卖出弹窗查询
- RealtimeMonitorService.java：evaluateAndAct()中用真实持仓数据计算“同日保护”——
    止损/抛压信号触发时若持仓中有当日买入未过户部分，在推给AI复核前就把
    “持仓X股中有Y股为今日买入尚不可卖”写入note，AI和最终通知都能看到
- WatchlistManager.java：toJson()补充divKLow（三级止损最低点），止损位三档
    （中点/最低点/前阳低）现在都能被前端拿到
- index.html：
  - 修复bug：WATCHLIST_STATUS_LABEL/PENDING_STATUSES缺失FULL/PENDING_FULL/
    PENDING_WARN，此前后端已支持的“建议满仓/建议抛压”信号在前端会显示异常
  - 持仓卡片：lockedQty>0时显示🔒锁定徽章
  - 卖出弹窗：默认卖出数量按可卖数量（而非持仓总量）封顶，锁仓时在modalAdvice
    提示；confirmTrade()对SELL也按sellableQty封顶，且区分-2(T+1拒绝)与-1(其他)
    两种失败给出不同提示

已知局限（如实记录，非隐瞒）：
- 涨跌停幅度按代码前缀简单区分（主板/创业板科创板/北交所），未做ST精确识别
  （依赖“ST”字样在股票名称中，未接入官方板块/风险警示接口）
- 疑似封板判断是启发式代理，不是真实盘口挂单量，可能有误判（宁可提示保守一些）
- T+1可卖数量用“总持仓-今日买入量”简化计算，假设卖出总是先消耗更早买入的部分，
  未做逐笔lot级别精确追踪（对当前场景够用，多笔分批买入+同日部分卖出的极端情况
  未做穷举测试）

四、前端需配合的改动清单 —— 完成度：
- 候选池卡片展示水线/VWAP/量比/止损位/观察天数/T+1状态：基本完成
  （水线/VWAP/量比现在有独立结构化字段了，不再只靠lastNote文本；止损位
  （divMidKline/divKLow/prevYangLow）早已进入toJson；持仓卡片有T+1锁定徽章）
- 状态标签新增PENDING_FULL、一级预警、待确认·满仓：✅ 本次已修复
- 监控面板收盘前30分钟倒计时、参数配置入口：⏳ 未做
- 持仓页展示关联监控状态：⏳ 未做（现状：持仓页只有T+1徽章，无监控状态展示）
- 决策日志记录量比/VWAP/分歧K线位/止损级别：✅ 已完成（早前会话，DecisionLogger
  记录ruleResult.metrics/note全文）
- 通知文案区分底仓/加仓/满仓/止损级别 + T+1不可卖警告：✅ 已完成

### 阶段 7：AI层适配 —— 大部分完成

verifySignal()的prompt已包含：规则依据(ruleNote，含本次新增的T+1/涨跌停提示)、
量化指标(metrics)、实时行情、近期走势、历史交易、当前动作类型(action/actionLabel)。
尚未做：①按判断类型分类注入话术（现在是全量注入知识库，非按买底仓/满仓/止损/
诱多识别分类）；②AI“不确认”样本定期复盘、高频原因下沉为Layer 1硬规则的
反馈机制——这是一个需要人工定期查看决策日志的运营流程，不是单纯代码改动，
待你有意愿时再讨论怎么做。

### 阶段 8：进阶能力 —— 按计划暂不做

正T机制、时间窗口放量分级、大盘过滤器、ATR止损缓冲、形态二识别、移动止盈
均未实现，符合文档“前6步落地后再回测验证”的建议，且形态二按你的决策
“暂不实现，仅观察池”。

### 2026-07-26：用户自行修复的3个真实bug + 补全PatternRef完整链路

【背景】用户在上一轮基础上自行修改了 WatchlistManager.java 和 TradingRuleEngine.java，
修复了三个此前一直存在、但未被发现的真实bug。经复核，三处修复方向和实现都正确：

1. 【满仓“吃影线”取错日期】evaluateFullPosition 原来动态用
MarketDataManager.getCachedKline(code,3) 取“最新缓存日”当仙人指路形态日算上影线——
但股票入池后可能在WATCHING状态躺好几天才等到条件成立，或中途重新跑过盘后下载，
“最新缓存日”早就不是当初真正出现长上影线的那一天了。修复：WatchlistManager新增
pattern_open/high/close/low/date五个字段（DB_VERSION 3→4），入池时固化仙人指路
当天的OHLC（PatternRef），之后不再变化。

2. 【一级预警“当日峰值涨幅”跨日不清零】peakGainPct原来只涨不跌、从不重置，持仓
好几天后“当日峰值”其实是好几天前的旧值，一级预警的回撤比较全错位。修复：新增
peak_gain_date字段，每次评估先比对是否还是同一天，不是就先清零重算。

3. 【分歧K线识别永远慢一拍】detectDivergenceKline 原来用
MarketDataManager.getCachedKline(code,2) 取“今日”数据——但这个缓存只有手动
“盘后下载”才会更新，盘中tick循环从不触发下载，所以盘中拿到的“最新缓存日”实际上
永远是昨天（或上次下载时）的已收盘K线。改成直接用quote的实时open/high/low/price
重构“今天这根还在形成中的日K”，才能真正在盘中实时识别到文档1.4/5.3要求的
“上涨途中再度放量、收十字星”。

（顺带确认：MarketDataManager.getCachedKline() 之前用“ORDER BY trade_date ASC
LIMIT ?”，缓存条数超过limit时会取到最早的limit条而非最新的——如果保留了365天历史，
只要最近5条却取到一年前数据，会让整个引擎的“前一日参考价”全部算错。这处已经是
DESC+反转的正确实现，复核确认无需再改。）

【发现的问题】用户这次的改动方向和思路都对，但没串联完整，改完后项目当时是
编译不过的：

- TradingRuleEngine.java 里 evaluateFullPosition 的注释块中间混入了一行裸露的
  `MARKER_A`（没有//前缀），非法Java语句，直接编译报错；旁边还有个混入的“镱”字
  （生僻字，读不通）
- evaluateFullPosition 方法签名已经改成 (quote, PatternRef pattern, ...)，但
  evaluate() 内部调用它的地方参数还是旧的 (code, quote, prevDay, ...)，参数
  个数和类型完全对不上
- copyState() 忘了拷贝新加的 peakGainDate 字段——每次tick评估都会重新
  copyState()，漏拷贝意味着每次都会被判定为“跨天了”，peakGainPct 每个tick都被
  清零重算，刚修的bug2等于白修
- evaluate() 方法本身没有 PatternRef 参数，PatternRef 根本没有入口传进来
- 最后一环没打通：选股结果的JSON（MarketDataManager.runTDXFormula）没有输出
  形态日的open/high/low，只有收盘价；RealtimeMonitorService 和 StockBridge 的
  选股回调也还在调用旧4参数 addIfAbsent——就算前面全接好，新选出的候选股
  pattern_high 永远是0，满仓信号永远没法触发

【本次修复】

- TradingRuleEngine.java：删掉MARKER_A、去掉“镱”字并补完过渡句、copyState()
  补拷贝peakGainDate、evaluate()签名新增PatternRef pattern参数、
  evaluateFullPosition调用点同步新签名
- RealtimeMonitorService.java：evaluateAndAct()新增
  WatchlistManager.get().loadPatternRef(item)，传给mEngine.evaluate(...)
- MarketDataManager.java：runTDXFormula的选股结果JSON补上
  patternOpen/patternHigh/patternClose/patternLow/patternDate（取自数组最新
  一天i，即选股逻辑判定仙人指路信号的那一天）
- StockBridge.java：runRealScreener的onResult回调改用新的9参数
  addIfAbsent，读取上面新增的pattern字段传入

至此PatternRef完整链路（选股产出形态日OHLC → 入池固化 → 监控时读取 →
满仓判断使用）已经打通，全部改动文件做过括号配平检查+跨文件签名/字段引用
交叉核对，未发现遗留的编译问题。

【已知局限/后续注意事项】
- 这次DB schema从v3升到v4，是新增字段不是重建表，历史遗留的候选池记录（在
  这次修复之前就已经入池、还没转成STARTER/ADDED的WATCHING状态股票）不会被
  自动回填pattern_*，这些老记录的patternHigh会一直是0，对应的满仓判断会一直
  跳过，直到该记录被移除重新入池，或数据库重置。这是schema升级不做历史回填的
  正常代价，不是bug，但如果发现某支老候选股迟迟不触发满仓，可以先查一下它是不是
  这种情况
- 本轮排查方式主要是静态代码审查+括号平衡/签名一致性检查，容器内没有javac，
  没能做真正的编译验证，建议你下次在Android Studio里实际编译一次，确认
  没有我没覆盖到的边界情况。

### 2026-07-26（续）：话术按判断类型分类注入 —— 已完成

对应阶段7剩余项之一“按判断类型注入话术（非全量）”。改动文件：

- WisdomManager.java：DB版本1→2，新增category字段。【重要】原本onUpgrade是DROP TABLE
  重建，升级会把用户已教过的话术全部清空，改为ALTER TABLE增列保留已有数据。
  addEntry/buildInjectBlock都新增了带category参数的重载，不传退化为旧行为
- LocalAIAgent.java：buildLearnPrompt改为让AI同时输出分类标签（BUY_STARTER/ADD_HALF/
  BUY_FULL/WARN_PRESSURE/STOP_LOSS/GENERAL）+复述两行；parseCategoryOrGuess解析，
  失败或模型不可用时guessCategoryByKeyword关键词兜底；verifySignal复核时通过
  getSystemPrompt(action)只注入“通用+匹配当前判断类型”的话术，不再无差别塞全部；
  buildScreenPrompt/buildChatPrompt仍用不过滤的旧行为（这两个场景没有具体判断类型）

已知局限：分类依赖AI自己汇报或关键词启发式推测，不是精确分类；历史话术（升级前
已教过的）category默认为空，会被当作“通用”对待（任何判断类型都会注入），不会因为
旧记录没分类而被漏掉。

### 2026-07-27：两AI并行开发后的完整性审查

【背景】本轮起项目由两个AI并行修改，需要确认没有相互覆盖/冲突。审查方法：
重新完整读取LocalAIAgent.java/TradingRuleEngine.java/WisdomManager.java/
RealtimeMonitorService.java全文，用grep定向核对WatchlistManager.java/StockBridge.java/
MarketDataManager.java的关键方法签名，全部做括号配平检查。

【审查结论：七个文件全部确认一致，未发现冲突】

- 本（Claude）做的话术分类注入功能（WisdomManager DB v2、LocalAIAgent的
getSystemPrompt(actionKey)系列重载、buildLearnPrompt分类输出）完整无损，未被覆盖
- 另一个AI在LocalAIAgent.java里做了不同维度、不冲突的改动：buildVerifyPrompt新增了
大盘环境注入（MarketIndexManager.getMarketSummaryText()，取值失败静默跳过不影响主
流程）；StockExpertSystem的降级文案去掉了旧式固定百分比挂单/止损价算法（和
现在实际监控用的水线+VWAP+分歧K线体系对不上，容易误导用户），改为文字说明。
这个方向是对的
- TradingRuleEngine.java、WatchlistManager.java、RealtimeMonitorService.java、
MarketDataManager.java、StockBridge.java、WisdomManager.java 五个文件都与我上轮
结束时的状态完全一致，未发现另一AI对这几个文件有实质性修改

未发现任何需要回滚或修复的冲突，两边改动都是加法关系。

### 2026-07-27（续）：候选池水线/VWAP/量比结构化展示 —— 已完成

对应“四、前端需配合的改动清单”第一条里“候选池卡片展示水线/VWAP/量比”，
之前只有lastNote文本形式，现在补上结构化字段：

- TradingRuleEngine.java：RuleResult新增waterLine/vwap/volRatio三个字段，在
  止损/满仓/加仓/底仓四个有动作的返回分支都填充，用的是evaluate()里本来就有
  的本地变量，不重新计算
- WatchlistManager.java：新增内存态sLiveMetricsCache（不落库，重启清空），
  updateLiveMetrics()写入，toJson()合入这三个字段
- RealtimeMonitorService.java：评估后调用updateLiveMetrics()，只在
result.waterLine>0（即本轮真正算出了有效值）时才写，避免用0覆盖掉之前的
好数据
- index.html：候选池卡片在lastNote下面新增一行水线/VWAP/量比展示，三个值都是0
或缺失时不显示这一行

已知局限：这个缓存是纯内存态，App重启后要等下一轮监控tick才会重新填充，
重启瞬间候选池卡片会短暂看不到这三个数字，属于正常现象不是bug。

### 2026-07-27（续）：收盘倒计时 + 持仓页监控状态 复核确认 / 参数配置入口 补全

对应"四、前端需配合的改动清单"里"监控面板收盘前30分钟倒计时、参数配置入口"和
"持仓页展示关联监控状态"这两条。开始动手前先核实了一遍现状（有过传话描述"已完成"
但文档没记录，不能直接采信），结论：

【核实结果】
- 收盘倒计时：index.html的formatCloseCountdown()已存在且实现正确——纯前端用
  设备时间计算，周末/非收盘前30分钟窗口正确返回空字符串，正确挂在onMonitorTick
  里跟着状态栏一起刷新。不需要改动，确认完成。
- 持仓页监控状态：renderPositions()已经用STATE.watchlistItems.find(w=>w.code
  ===code)正确交叉引用候选池数据，展示监控状态标签+说明文字；数据链路
  （loadWatchlist()→STATE.watchlistItems→renderPositions()）完整可用。
  不需要改动，确认完成。
- 参数配置入口：TradingRuleConfig.java的两层加载（内置默认值assets/
  trading_rules.json + 用户可写覆盖文件trading_rules_override.json，load()/
  applyUpdates()/toJson()/saveOverrides()）确实已经实现——但StockBridge.java
  完全没有对应桥接方法，index.html完全没有配置面板UI，功能没有真正打通，
  用户实际上还用不了。这部分是本次要补完的。

【本次新增】
- TradingRuleConfig.java：新增resetToDefault(ctx)——删除用户覆盖文件并把当前
  单例重新加载为纯内置默认值，同样立即生效不需要重启App（saveOverrides此前
  已支持"改了立即生效"，这次补的是反向操作）
- StockBridge.java：新增三个桥接方法 getTradingConfig()（读当前生效值）/
  saveTradingConfig(jsonStr)（只需传有改动的字段）/ resetTradingConfig()
- index.html：
  - 实时监控面板新增"⚙️ 参数配置"按钮（和已有的🔔测试通知/📝决策日志按钮同一行，
    风格一致），点击展开可折叠面板，复用decisionLogPanel的交互模式
  - TRADING_CONFIG_FIELDS数据驱动18个可调参数（除marketCloseHour/Minute——
    A股收盘时间固定15:00，不是真正的策略阈值，没放进UI，字段本身在Java端仍完整
    保留），按"放量确认/买入确认/止损止盈/涨跌停幅度"四组展示，每项都带一句话
    说明这个参数具体影响什么
  - percent类型字段（吃影线比例/仓位比例/回撤比例/涨跌停幅度等）界面上按百分比
    展示和输入（乘/除100），避免用户要输入"0.3"这种不直观的写法；
    divergenceMidMode是下拉选择而非数字输入
  - "恢复默认"按钮直接执行不弹确认框——检查过这个App里没有任何地方用原生
    confirm()/alert()（removeFromWatchlist等其他"删除类"操作也是点了就直接执行+
    toast提示），跟着这个已有的交互习惯走，不引入新的弹窗模式
- 三处改动的字段名（Java的applyUpdates/toJson 对 JS的TRADING_CONFIG_FIELDS）
  逐一核对完全一致，做过paren/brace计数交叉核对确认改动本身内部平衡、没有破坏
  原有文件结构

已知局限：
- "恢复默认"没有二次确认，误触后需要重新手动调回想要的值（参数都还在界面上，
  不是不可逆，只是要重新输入）；如果你觉得有必要加确认，可以在下次开发时补一个
  非原生的自定义确认提示，不用confirm()
- 参数配置面板目前只做了"读取+保存+恢复默认"，没有做修改历史记录（比如"上次是什么
  时候改的、改了什么"），如果需要追踪参数调整历史，这个待后续讨论
- 本轮同样只做了静态审查+字符串精确匹配替换，容器内无javac，建议和前面几节一起，
  下次在Android Studio里统一编译验证一次

### 2026-07-30：数据实时性 + 仙人指路形态 + AI逻辑可控性 专项审查

【背景】你要求专项核查：买卖监测和AI逻辑是否符合操盘手经验终版.md，仙人指路形态
判断是否准确，所有数据是否真正实时，AI能读到的数据是否准确不陈旧。

【仙人指路：选股公式保持不变，新增独立可选开关】

文档3.1节精确定义：上影线长度≥实体长度×2，且当日成交量≥5日均量×阈值倍数。之前
runTDXFormula的condA/condB是另一套不相关的公式（EMA/SAR趋势判断），按你的要求不修改
这套既有公式，改为新增独立可选开关

- MarketDataManager.java：runRealScreener/runTDXFormula新增requireXianRenZhiLu参数，
  勾选时额外要求上影线≥实体×2 且 当日量≥5日均量×volMulti（复用已有的放量倍数参数）；
  不勾选时这两个值仍会算出来放进结果JSON（isXianRenZhiLu/shadowRatio）
- StockBridge.java：runRealScreener读取requireXianRenZhiLu并传入
- index.html：选股页新增勾选框“叠加仙人指路形态检测”

【发现并修复3个真实bug】

1. TradingRuleEngine.updatePrevYangLow()起点off-by-one——止损参照价“前一根阳线
最低价”的搜索原来从bars.size()-2开始，跳过了缓存里最新的一天。但盘中监控时这个
“最新缓存日”实际上永远是水线那天（昨天），不是今天；如果水线那天本身是阳线，它
应该是最贴近“前一根阳线”定义的那一根，之前的写法会把它跳过去找更早的一根，导致
止损参照价比应有的更低、触发得更晚。改为从bars.size()-1开始找。

2. LocalAIAgent.buildVerifyPrompt/buildChatPrompt跳过了ensureFreshBlocking()——
MarketIndexManager类文档明确要求先调用ensureFreshBlocking()确保大盘指数缓存不
过期，再调用getMarketSummaryText()。选股用的buildMarketContext()正确调用了，但
AI复核买卖信号（verifySignal）和聊天这两条路径直接读缓存，从不主动刷新。已补上，
最长等6秒，超时用已有缓存（不是编造数据），不影响主流程。

3. index.html选股参数键名不匹配——前端SCREEN_PARAMS用的是excludeCY/excludeKC/
excludeLT/excludeST（H5模拟模式遗留），但真机走Android.runRealScreener时Java端
StockBridge读的是exCY/exKC/exLT/exST，两边一直没对上。默认值都是true（排除），
所以实际效果是：用户点“取消排除创业板/科创板/ST/涨幅≥20%”这几个开关，在真实
选股里从未生效过，Java端拿到的永远是optBoolean的默认值true。已修复，两套键名同时
发送，不影响H5模拟模式。

【AI逻辑可控性：新增复核外层超时兜底】

复核RealtimeMonitorService的AI调用处理逻辑，架构本身是“失败即不推送”的保守设计：
onError时vr.confirmed=false；决策日志无论AI确认与否都完整记录；AI只有“确认/不
确认”一个输出通道，不能绕过规则引擎自主发起任何操作。但发现一个边界缺口：如果本
地模型JNI层面彻底卡死（既不触发onToken也不触发onFinish），onComplete和onError都
不会被调用，这次信号评估会静默消失，连决策日志都不会写。新增AI_VERIFY_TIMEOUT_MS
（50秒，比LocalAIAgent内部看门狗45秒稍长、比默认tick间隔60秒短）外层超时兜底，确保
任何情况下这次评估都有“超时不推送”的明确结论写进决策日志。

顺带补了一个透明度改进：AI看到的“实时行情”补上时间戳，方便日后从日志直接确认
数据新鲜度。

【确认清洁、无需改动的部分】

RealtimeQuoteManager无内部缓存，每次调用真实发起网络请求（双源+熔断）；
RealtimeMonitorService的tick循环每轮（默认60秒）都重新fetchBatch+fetchMinuteLine，
规则引擎和AI用同一份当轮数据，不复用旧数据；checkVolume的5日均量计算循环边界
正确排除“今天”；getPrevDayRef用缓存K线架构正确（水线定义就是“T日收盘价”，本质
是历史参照）；此前两轮已修复的getCachedKline排序bug和detectDivergenceKline实时
重构依然生效；parseVerifyResult的“确认/不确认”解析用“不确认”子串做安全网，AI
输出自相矛盾时也会保守地判定为不确认；DecisionLogger对超时/错误场景的日志文本处
理得当，不会因为特殊内容出问题。

全部改动文件（MarketDataManager.java/StockBridge.java/TradingRuleEngine.java/
LocalAIAgent.java/RealtimeMonitorService.java/index.html）逐一做过括号配平检查
+跨文件参数/方法签名交叉核对，未发现遗留问题。容器内仍无javac，建议找机会在
Android Studio里做一次真实编译

### 2026-08-05：候选池清理机制 + 规则/AI推送解耦 —— 审查确认 + 1处真实bug修复

【背景】上一条第二轮修改方案思路需具体审核.md第1条指出"风险信号不该被AI卡住"，
你据此自行推进了一版改动，范围收窄为：候选股清理机制（跌破水线/跌破前阳低/观察
超时三条件）+ WatchlistManager加ai_status异步状态位+RealtimeMonitorService.
evaluateAndAct整体重写（规则命中立即推送，AI改为纯异步补充）。本轮任务：审查这部分
改动是否准确，补完剩余实现计划；第二轮文档里其他条目（风险预算仓位管理等）本轮不动。

【审查结论："已完成"部分全部核实无误】

- TradingRuleConfig.java + trading_rules.json：candidateDeepBreakPct(0.08)/
  candidateMaxObserveDays(10)两个字段与JSON默认值一一对应，注释如实标注"建议值
  需回测"
- TradingRuleEngine.java：WATCHING状态分支在evaluateStarter前会先调用
  updatePrevYangLow(code, state)，复用止损同款的独立破位逻辑；starter.stateUpdate
  = state把这个值带出evaluate()，供候选股清理机制使用，链路正确
- WatchlistManager.java：DB_VERSION升到5新增ai_status列（默认'NONE'）；
  markPending()确认是规则命中时立即调用的4参数版本；updatePendingAiResult()里
  `if (cur == null || cur.pendingAction == null) return;`这行保护确认存在，用户
  抢先确认/忽略后AI结果不会覆盖；autoRemoveStale()按预期把状态打成MANUAL_REMOVED
  并在last_note写清楚自动移除原因

【审查结论："剩余实现计划"三项其实已经全部做完，不是待办】

写这轮需求时可能上一轮本地修改已经顺手做完了，没同步更新"已完成"清单，逐项核实：

1. RealtimeMonitorService.evaluateAndAct已是重写后版本：action==NONE时只
   updateNote+判断候选股清理；action!=NONE时先applyT1Note（已抽成独立方法），
   再shouldNotifyNow()判断推送窗口（只有STOP_LOSS的LEVEL2_MID会因不在收盘前
   窗口返回false），不在窗口只updateNote+return，不markPending不入AI队列；在
   窗口才markPending+写决策日志(logRulePush)+fireAlert+入队。checkStaleCandidate
   已实现且正确使用result.stateUpdate.prevYangLow（不是loadTrackState()）——
   额外做了代码追踪确认这个修复真实有效：loadTrackState(item)不查库，只是从
   传入的WatchlistItem对象上直接取字段值，而这个item对象是本轮tick一开始从
   getActiveWatchlist()取出的快照，此时还没经过本轮evaluate()里
   updatePrevYangLow()的计算，如果真用loadTrackState()确实会慢一拍，原来的
   诊断成立，当前实现是对的。handleVerified已重命名为handleAiAnalysisComplete，
   3处调用点（超时兜底/onComplete/onError）全部同步更新，职责收窄为只回填
   ai_status+写补充日志，不再决定是否推送
2. index.html：TRADING_CONFIG_FIELDS已含"候选池清理"分组的两个新参数；
   renderWatchlist()里aiStatusTag已按PENDING/CONFIRMED/DOUBTED/默认四种状态
   显示"AI分析中…/AI分析已完成·支持/AI分析已完成·存疑/等待AI分析"
3. WisdomManager.java：ensureDefaultWisdom()已实现，构造函数里hasAny()为空时
   预置6条默认话术，按BUY_STARTER/ADD_HALF/BUY_FULL/WARN_PRESSURE/STOP_LOSS/
   通用分类，内容体现"买入偏审慎复核、止损偏支持规则执行"的心态设计；
   LocalAIAgent.getSystemPrompt(actionKey)→getTradingKnowledge(actionKey)→
   safeWisdomBlock(actionKey)→WisdomManager.buildInjectBlock(actionKey)这条
   注入链路核对完整，选股/聊天场景用null（不过滤）符合预期

【跨文件依赖交叉核对，未发现签名不匹配】

RealtimeMonitorService本轮重写涉及的外部调用点全部核对参数个数/类型/顺序：
DecisionLogger.logRulePush(7参数)/logAiSupplement(6参数)、LocalAIAgent.
verifySignal(8参数，含Quote和AICallback)/VerifyResult字段/parseVerifyResult、
DatabaseManager.getSellableQuantity/getAllPositions/getPositionByCode、
Position的四个getter、StockBridge的getTradingConfig/saveTradingConfig/
resetTradingConfig/getSellableQuantity桥接方法，逐一读取对应文件确认签名一致；
StockMasterApp.onCreate()里7个manager的init()顺序也确认TradingRuleConfig在
WatchlistManager/DecisionLogger之前，不存在初始化时序问题。

【发现并修复1个真实问题：候选股观察天数按日历天算，和文档口径"交易日"不符】

RealtimeMonitorService.computeObserveDays()原实现是两个Date相减除以一天毫秒数，
本质是日历天数；但trading_rules.json的candidateMaxObserveDays注释、
TradingRuleConfig.java字段注释都明确写的是"交易日"。日历天数会把周六周日也计入
观察期——比如周五入池，中间跨一个周末，到下周一才过了2个交易日，但日历天数已经
算到第4天，10天的口径在跨周末较多时会提前1~2天把候选股误判超时移出。index.html
renderWatchlist()里"观察N天"这个展示用的是同一个日历天算法（前端自己单独算的，
没复用后端逻辑，两边一起偏差）。

已改为按周一到周五估算交易日，Java端computeObserveDays()和JS端新增的
computeTradeDaysSince()保持同一套简化口径：不接入法定节假日日历，理由和本类
isWithinTradingHours()、MarketDataManager.computeExpectedTradeDate()一致——
节假日当天本来也不会有行情变化，顶多把移出判断推迟一两天，不会误报，是这个
项目一直以来对"交易日"的统一简化处理方式，没有引入新的不一致口径。

【已知局限】

- 交易日计算不接入节假日日历，国庆/春节等长假期间，观察天数会比真实交易日数
  偏多（虚高）几天——比如10天参数横跨国庆7天长假时，实际只经过3个交易日却已经
  计满10个"周一到周五"天数。这是当前简化方案的已知代价，如果要精确处理需要接入
  真实交易日历数据源，超出本轮范围，记录在此供后续参考
- candidateDeepBreakPct(8%)、candidateMaxObserveDays(10天)、prevYangLow独立破位
  三条候选股清理规则目前都还没有历史数据回测验证误清理率，和文档里其他"建议值"
  一样需要后续用真实数据跑一遍
- 本轮同样只做了静态审查+跨文件签名核对，容器内无javac也没有Android SDK，无法
  做真实编译，建议下次在Android Studio里跑一次Build，重点看
  RealtimeMonitorService.java（本轮唯一有逻辑改动的文件）



