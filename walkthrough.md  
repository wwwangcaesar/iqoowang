真实行情数据与选股公式适配完成
按照您的需求，App 现在已经能够独立拉取真实的历史行情数据，并在本地计算通达信公式选股了！

关键更新点
1. 本地历史行情数据库 (MarketDataManager)
新增了独立的 MarketDataManager 模块，它完全在 App 本地（SQLite）运行，执行以下通达信标准的"盘后数据下载"流程：

全量获取：通过东方财富接口拉取沪市、深市主板全量股票列表（自动翻页）。
基础预筛：下载时剔除了ST股、非主板股票，并按照您设置的价格区间（如 3-50元）、市值区间（如 20-320亿）进行候选股缩减，节约下载时间和流量。
并发下载：开启 8 线程并发下载候选股的 前复权 日 K 线数据（保存至少 35 天，满足 MA25 和 SAR 等技术指标的计算需求）。
空间估算：提供下载前的容量估算（大约 3-4 MB），以及下载进度条展示。
滚动清理：每次下载会自动清理超过 35 天的过期 K 线缓存数据，避免存储体积膨胀。
2. 选股页面 UI 升级
在"选股"页的公式下方加入了新的**「盘后数据下载」卡片**：

估算大小：点击可直接查看按照当前过滤条件，预计下载的股票数量和体积。
进度展示：点击"下载历史数据"会触发多阶段的进度条显示： Phase 1（拉取列表） -> Phase 2（预筛选） -> Phase 3（并发下载K线）。
状态联动：只有在数据下载完成后，"运行选股"按钮才会真正起效。
3. 真实通达信公式运转
runTDXFilter 逻辑已经从原本的前端 JS 模拟改写到 Java 本地的 MarketDataManager.runRealScreener。

它现在读取本地 SQLite 中真实的 K 线价格和成交量。
计算出真实的 EMA、MA、STD 和 SAR 指标。
判断条件完全映射您要求的"放量突破"或"缩量持续"的买入信号。
验证步骤
为了验证这些功能的实现，请您重新编译并运行 App：

打开选股页面 点击底部的「选股」导航栏，此时页面上方应该出现一个新的「盘后数据下载」卡片，状态显示"未下载"。
下载盘后数据
先点击 [估算大小] 按钮，确保弹出提示类似 "候选 800支, 网络约 3.2MB"。
接着点击 [⬇ 下载历史数据]。注意观察进度条从拉取列表到下载 K 线的过程。
运行选股
下载完成后，卡片状态会变为"已就绪"。
点击 [⚡ 运行选股] 按钮。
这一次，选股将基于真实的市场数据和通达信公式运行。您可以看到最终选出的真实股票结果，且 AI 引擎会自动对结果进行深度解析！
TIP

目前模型分析使用的是 H5 备用规则流（如果 Qwen 还在加载），这使得交互始终顺畅。当 JNI 中的 Qwen 准备好时，底层会自动切换到本地大模型思考流。

请您体验一下最新版本，看是否符合您的预期！如果您觉得筛选条件过严或太松，可以在页面直接修改价格/市值/板块选项，然后重跑即可。